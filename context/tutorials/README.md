# Graphia Tutorials

Per-increment learning tutorials for [Graphia](../../README.md). Each one covers a single completed spec increment — teaching **only** the concepts that increment newly introduced, **depth-first** (deepest idea first, decorations last) and **Socratic** (pose a design question → name the framework feature that answers it → apply it with a real snippet). Concepts already taught by an earlier tutorial are *referenced, not re-taught*, so the set reads cleanly front to back.

Each tutorial ships with a companion `concepts.md` — a ledger of the concepts it introduced (human-readable title + a stable kebab-slug). That ledger is the dedup mechanism: a later tutorial reads all earlier ledgers and skips anything already covered.

> These artifacts are produced with the [`/awos:tutorial`](../../.awos/commands/tutorial.md) skill (an extension to the [AWOS](https://github.com/provectus/awos) workflow). See the [project timeline](../project-timeline.md) for how the increments unfolded and the [roadmap](../product/roadmap.md) for what's next.

---

## Read in order

| # | Tutorial | What you'll learn | Concepts | Prereqs |
|---|----------|-------------------|:--------:|---------|
| 001 | [**Playable Skeleton**](001-playable-skeleton/tutorial.md) | The core LangGraph paradigm — typed state with field-level reducers, conditional-edge routing, replay-safe `interrupt()` + `Command(resume=…)`, streaming super-step updates, a per-thread SQLite checkpointer, flat-Pydantic structured LLM output, and the Textual TUI. The foundation everything else builds on. | 20 | none |
| 002 | [**Hosted AgentCore Deployment**](002-hosted-agentcore-deployment/tutorial.md) | Taking the *same* graph to AWS Bedrock AgentCore — the managed-container Runtime, a Gateway-fronted diary tool surface, AgentCore Memory, Terraform-in-a-container, a mode-agnostic local/remote driver, observability trace trees, and the failure modal. | 38 | 001 |
| 004 | [**Robust /vote Input Validation**](004-robust-vote-input-validation/tutorial.md) | The LangGraph interrupt/resume-**pump contract**: why a node must `interrupt()` at most once per execution, the driver "empty `next` masks a pending interrupt" gotcha that ended a game on a typo, re-prompting via a state channel + conditional-edge loop, and testing through the *real* driver. A great standalone read. | 5 | 001, 002 |
| 005 | [**Play-As-Role via Environment Variable**](005-play-as-role/tutorial.md) | The project's **determinism posture as policy** — `GRAPHIA_ROLE` to pin the human's side (pop-then-shuffle that preserves the lineup by construction), pinning roles in tests via `setenv` instead of magic seed values, monkeypatching the shuffle helper for a fixed order, and the full retirement of `GRAPHIA_SEED` from production. | 9 | 001, 002 |
| 006 | [**Long-Term Cross-Game Memory & Career Stats**](006-cross-game-career-stats/tutorial.md) | **Event sourcing on AgentCore Memory's two tiers** — per-action events as the source of truth, a long-term record as their `fold`ed consolidation — with one shared pure `fold` guaranteeing local↔remote parity, a self-managed strategy + SNS→Lambda consolidation pipeline (with `games_folded` idempotency), `partial`-based service injection behind a shared graph assembler, and a loud-fail posture. Opens with a candid note that the pipeline is *deliberately over-engineered* for a stats counter. | 16 | 001, 002 |
| 007 | [**Fair Day Speaking Order**](007-fair-day-speaking-order/tutorial.md) | **The testing craft of proving a random invariant** — reproducible seeded sampling from one stable base seed; structural independence by exact same-seed equality vs statistical uniformity within a σ-justified band; the chokepoint-plus-integration two-altitude strategy; a tests-only guard-rail spec; and the harness tricks for running real games offline (an unbounded structured-output LLM stub, per-game checkpoint isolation, stable-label aggregation). | 8 | 005 |
| 008 | [**Same-Round Message Visibility**](008-same-round-message-visibility/tutorial.md) | **One guarantee, two delivery mechanisms** — the AI's view of the Day round is a bounded prompt window while the human's is the unbounded on-screen log; one context chokepoint feeds both the speak and vote prompts; sizing the window to a domain invariant; a constant-floor guard assertion that turns a magic number into a tripwire; and a race-safe UI test that reads the screen at the human's turn under neutralized RNG. | 5 | 007 |
| 009 | [**AI Collusion Awareness**](009-ai-collusion-awareness/tutorial.md) | **How to evaluate and fix non-deterministic LLM behaviour** — the shipped change is one prompt line; the lesson is the paired-seed real-model A/B that chose it: a make-gated eval outside the mocked suite, name-masked similarity (template echo), a length-cap confound control, bootstrap CIs + paired ranking with Holm, in-process factor injection, GenAI-span trace archaeology, and the headline that rigor *reversed* the noisy pilot's verdict (prompt-priming backfire → anti-parrot self-instruction). | 10 | 007 |
| 010 | [**Local Ollama Provider**](010-local-ollama-provider/tutorial.md) | **A second LLM backend behind one seam** — the provider strategy at the tier factory (zero call-site changes, landed as a zero-behavior refactor); driving a local Ollama model through *Anthropic's* Messages protocol; gating the unproven transport on measured structured-output reliability — counting raw tool-call outcomes *underneath* the game's retry/fallback masking (a 100%-failure model still "completes" a game); offline-by-construction config gating; and a fail-fast preflight with fix-it commands. | 8 | 001, 009 |
| 011 | [**AI Blunder Tracking**](011-ai-blunder-tracking/tutorial.md) | **Measurement discipline for a stochastic system** — turning AI quality into a tracked property via a repo-committed, append-only YAML ledger ("baby MLOps"); run provenance (git commit + dirty flag, model digests, metrics_version) that makes a number attributable; *capturing* a blunder the game's validator rejects before it reaches state (extending 010's counting proxy), attributed by parsing the invoke prompt to dodge a stale-`get_state` trap; template-derived parsing as a reword tripwire; absent≠zero; a Wilson confidence interval per rate; and the n=3→n=20 baseline that *reversed* the small-sample conclusion. | 8 | 009, 010 |
| 012 | [**Eval Ledger Viewer**](012-eval-ledger-viewer/tutorial.md) | **A pure core under a thin TUI shell** — reading spec 011's append-only quality ledger as a scrollable, searchable Textual `DataTable`: a Textual-free data layer that emits plain strings (all parsing/formatting/search unit-testable without a TUI) under a thin viewer; reading a *heterogeneous* file (pre-provenance vs full records) through one defensive dotted-get so a missing field never crashes the UI; `safe_load_all` making it read-only by construction (the YAML dep 011 deferred); an index-parallel table model + one `METRIC_ORDER` source of truth; cell-cursor pan-into-view; push/pop drill-down with cursor restore; selector-scoped search with no `field:value` syntax; and a table-first focus model. | 11 | 001, 011 |
| 013 | [**AI Behavioral Integrity**](013-ai-behavioral-integrity/tutorial.md) | **Fixing an agent's behaviour as a tested hypothesis** — the rare tutorial about an *in-flight* increment (one hypothesis confirmed, one refuted, one open — that's the lesson): a behaviour fix is a hypothesis the before/after measurement confirms or refutes, with a baseline committed on the unchanged behaviour *first*; outcome + vote-activity tracking (win-rate by side, activity by game-day) so a dead Day phase becomes a number; explicit-zero vs absent-omission as deliberate inverses; role/identity grounding injected into every prompt; and the knowledge-boundary invariant that a Citizen never learns another player's allegiance. | 7 | 009, 011 |
| 014 | [**Configurable Role Counts**](014-configurable-role-counts/tutorial.md) | **Holding an invariant across a stochastic boundary** — making the lineup configurable means the role deck and the AI roster must be the *same* size every game or the deal `IndexError`s: derive both from one config (the single-source size invariant); refuse an unworkable lineup up front with fail-fast validation whose `mafia < citizens` rule encodes the game's parity-favours-Mafia win check; relax the `Roster` schema to a range and move exact-count enforcement to the caller; put a deterministic `_coerce_to_count` floor under the validation-retry so a small model that can't count never breaks the invariant; and record the lineup as a comparable eval variable surfaced in the 012 viewer. | 7 | 001, 005, 010, 012 |
| 015 | [**Multi-Round Mafia Consensus by Pointing**](015-multi-round-mafia-consensus/tutorial.md) | **Shaping a human-in-the-loop loop so resume recomputes nothing** — the Night kill becomes a bounded multi-round consensus, and LangGraph's "re-execute the whole node on resume" rule forces the design: a **per-pointer** loop (one actor's pick per super-step) with the round's randomness quarantined in its own no-`interrupt()` node, so a human-pointer resume re-derives committed state and re-invokes no AI pick; round-scoped loop state in committed channels; unanimity-or-cap termination resolving from the deciding round (old plurality+tie-break reused); per-round fair re-shuffle (the 007 pattern) so the last-mover advantage rotates; the running picks threaded into the AI prompt (Mafia-only, per 013) to induce convergence without chat; and a content-sized modal so the human sees the same context without the target list collapsing. | 8 | 001, 005, 007, 013 |

*(Tutorial **003** is intentionally skipped — Spec 003 "Reliable Game Exit Controls" shipped without a tutorial, and that index is left open rather than renumbering.)*

---

## Also here

- [`002-hosted-agentcore-deployment-v2/`](002-hosted-agentcore-deployment-v2/tutorial.md) — a **historical interim draft** of Tutorial 002 covering Slices 1–4 plus the Nova model switch, written *before* the ADR-005 pivot to Lambda-target Gateway tools. Superseded by the final Tutorial 002; kept as a record of the pre-pivot shape. Read 002 (final) instead unless you're specifically curious about the runtime-embedded-tools era.

---

## How a tutorial is structured

1. **Overview** — names the central technology/paradigm before any diagram.
2. **Concepts already covered** — prior-tutorial concepts this increment re-uses, linked back.
3. **What's new this increment** — an index of the new concepts (titles only), each linking down to where it's taught.
4. **Diagram** — a mermaid diagram when the increment changes something structural.
5. **Walkthrough** — the lesson: 5–8 sections, deepest concept first, each pose → present → apply.
6. **Try it** / **Where to go next** — hands-on pointer + the next thing to read.
