# Tasks: Role-Specific Day Guidance for AI Players (Spec 024)

Gameplay-influencing → gated by a **default-on** env flag `GRAPHIA_ROLE_GUIDANCE` so prior behavior is reproducible for A/B (ADR 011). Acceptance is **effort-not-results** (CR 005): the suite verifies structural injection + role-matching + the never-reveal invariant; the behavioral effect is eval-measured out-of-suite.

Functional spec: `./functional-spec.md` · Technical considerations: `./technical-considerations.md`

> **Shared edit surface with spec 025** (`_ai_day_action`/`_ai_ballot`, both Day templates, `config.py`, `graph.py`/`runtime/graph_builder.py` threading). Recommended order: **land 024 first** (smaller, pure prompt + flag, no provider question), then 025. Each spec appends its own keyword param to the partials, so they merge cleanly in that order.

---

- [ ] **Slice 1: AI players receive a role-specific closing directive in both Day prompts, ablatable**
  - [x] Add the role-guidance text + builder + template slots + wiring: two new per-side action-menu constants in `src/graphia/prompts.py` (a Law-abiding menu — spot a likely Mafioso, voice/accuse, put a genuine suspect to a vote, *without* getting fellow Law-abiding executed and without baseless accusation; and a Mafioso menu — hold the cover, deflect onto Citizens, shield/coordinate with teammates, steer votes, **never reveal**), each behind a short directive label; a new `{role_guidance}` slot at the **tail** of both `DAY_SPEAK_USER_TEMPLATE` and `AI_VOTE_USER_TEMPLATE` (after `Recent public discussion:` / the never-reveal line, at/just-before the final "take your turn" / "cast your ballot" line — recency); a pure `_role_guidance_block(role, *, enabled)` builder in `src/graphia/nodes/day.py` (mirrors `_standings_prompt_block`: returns the role-matched menu, or `""` when disabled); and a new keyword-only `role_guidance_enabled: bool = True` param on `_ai_day_action` / `_ai_ballot` that feeds `role_guidance=` into their `.format(...)`. **[Agent: langgraph-agentic]**
  - [x] Add the ablation flag + thread it through both graph builders: `GraphiaConfig.role_guidance_enabled: bool = True` parsed via `_env_flag("GRAPHIA_ROLE_GUIDANCE", default=True)` in `config.py`; `_assemble_graph` gains a `role_guidance_enabled` param bound (via the existing `partial(...)` pattern, alongside `recap_aware_reasoning_enabled`) into `day_turn` (→ `_ai_day_action`) and `collect_votes` (→ `_ai_ballot`); `build_graph` passes `config.role_guidance_enabled`; **`runtime/graph_builder.py`'s `build_runtime_graph` also threads it** (the named anti-drift requirement — both builders or local/remote diverge). **[Agent: langgraph-agentic]**
  - [x] Update the existing template-`.format()` test sites for the new `{role_guidance}` kwarg (grep the two template names to find the live set; at least `tests/test_behavioral_integrity.py`, `tests/test_blunder_eval_detectors.py`, `tests/test_instrument_capture.py`, `tests/test_personas.py`, and the slot guards in `tests/test_recap_aware_reasoning.py`) so no `.format()` raises `KeyError`; re-verify `test_behavioral_integrity.py::_DAY_SPEAKER_RE` still matches (tail placement keeps it valid). **[Agent: testing]**
  - [x] Add `tests/test_role_guidance.py` (structural-invariant only, mirroring `test_recap_aware_reasoning.py`): pure `_role_guidance_block` role-match (Citizen menu vs Mafia menu) + `enabled=False` → `""`; per-role guidance reaches **both** the speak and vote prompts for **both** roles via a content-capturing `get_large()` fake (the "all four cases"); a Citizen prompt never carries a Mafia-menu marker and vice-versa; the Mafioso prompt never instructs revealing side/teammates/cover; **tail placement** (block appears *after* `Recent public discussion:`); flag-off parity (both prompts revert to pre-024 form); `GRAPHIA_ROLE_GUIDANCE` default-on config semantics; template-slot guard; `build_runtime_graph` threads the flag (anti-drift). Full `uv run pytest -q` green. **[Agent: testing]**
  - [ ] Verification (effort-not-results, CR 005): _(deferred — run on merged main)_ `uv run pytest -q` green; then run `make blunder-eval` (provider of choice; Ollama free) and compare against the recorded baseline — win-rate by side, **votes initiated**, share **resolved vs `no_winner`**, plus a watch on **Law-abiding-executed-by-Law-abiding** (decisiveness not bought by citizens executing each other) — and log the hypothesis confirmed or refuted. Clean-or-commit transcripts before the measured run. _(Run on merged main if implemented in an isolated worktree.)_ **[Agent: testing]**
