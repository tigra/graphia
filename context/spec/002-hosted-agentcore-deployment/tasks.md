# Implementation Tasks: Hosted AgentCore Deployment

- **Functional Specification:** [`functional-spec.md`](./functional-spec.md)
- **Technical Specification:** [`technical-considerations.md`](./technical-considerations.md)
- **Related ADRs:** [ADR 001 — Hosted AgentCore Runtime with Preserved Local Mode](../../adr/001-hosted-agentcore-with-local-mode.md), [ADR 002 — Runtime-embedded Gateway Tool Handlers](../../adr/002-runtime-embedded-gateway-tool-handlers.md)

This task list is organised into vertical slices. Each slice leaves the app in a runnable state (local mode at minimum; remote mode from Slice 4 onwards) and ends with an automated test plus a user-performed manual smoke test where AWS state needs to be inspected. Slices build strictly on top of one another — do not skip ahead. Per ADR 001's dual-mode posture, local mode must continue to play a full game end-to-end at every slice boundary.

---

## Slice 1: Config refactor + auth posture (local mode preserved)

_The smallest piece of preparatory value: `GraphiaConfig` supports the new SSO-canonical auth posture (per `project_aws_account` memory) and remote-mode awareness, while local mode plays a complete game exactly as before. No AWS / AgentCore work yet._

- [x] Refactor `src/graphia/config.py`: make `bearer_token` optional (`str | None`, defaulting to `None` when `AWS_BEARER_TOKEN_BEDROCK` is unset); flip the `aws_region` default from `eu-north-1` to `us-east-1`; add `remote_mode: bool` (read from `GRAPHIA_REMOTE` env or `--remote` flag); add `runtime_invocation_url: str | None` (read from `GRAPHIA_RUNTIME_URL` env). Remove the `SystemExit` on missing bearer token; replace with a *contradictory-config* check (raise only if `remote_mode=True` and `runtime_invocation_url` is empty). **[Agent: python-backend]**
- [x] Update `src/graphia/__main__.py` to parse `--remote` from `sys.argv` via `argparse` and surface it as `GRAPHIA_REMOTE=1` in `os.environ` before `load_config()` is called. Keep all existing UTF-8 stdio reconfig + `.env` loading behaviour unchanged. **[Agent: python-backend]**
- [x] Confirm `src/graphia/llm.py`'s `get_sonnet()` / `get_haiku()` factories still work with `bearer_token=None`: boto3's default credential chain (e.g. `AWS_PROFILE` resolving an SSO profile) should provide credentials transparently to `ChatBedrockConverse`. No explicit code changes expected; this is a verification task. **[Agent: langgraph-agentic]**
- [x] Add `tests/test_config_auth.py`: parametrised tests covering (a) bearer-token-only env, (b) `AWS_PROFILE`-only env (no bearer), (c) both set, (d) `--remote` flag with `GRAPHIA_RUNTIME_URL` present, (e) `--remote` flag with `GRAPHIA_RUNTIME_URL` missing — must raise the contradictory-config error. **[Agent: testing]**
- [x] Verify automated tests pass: `uv run pytest tests/test_config_auth.py -q` and confirm the existing spec-001 slice tests still pass (`uv run pytest -q`). **[Agent: testing]**
- [x] **USER:** Manual smoke test — set `AWS_PROFILE=my-aws-profile` (and run `aws sso login --profile my-aws-profile` if needed) in the shell, *unset* `AWS_BEARER_TOKEN_BEDROCK` in `.env`, run `uv run python -m graphia` in a real terminal. Confirm a full game plays end-to-end without errors, identical to spec-001 baseline behaviour. _(Agent cannot reliably verify SSO session state; user-performed.)_

---

## Slice 2: AgentCore resource-name discovery + Terraform module skeleton

_The first IaC slice: confirm the actual `aws_bedrockagentcore_*` Terraform resource names from the Registry (Risk 3 from tech-spec §3.2) and lay down the bare Terraform module shell with versions, variables, and outputs. `terraform plan` succeeds against an empty deployment. No real cloud resources yet — this slice gates the rest of the IaC work._

- [x] Use `terraform-mcp-server` to query the `hashicorp/aws` provider's bedrock-agentcore resources: confirm exact resource names for Runtime, Gateway, Gateway target, Memory, and the IAM execution-role pattern. Save findings as `infra/terraform/RESEARCH.md` with provider version, confirmed resource names, and any fallback (e.g., `awscc` provider) needed for resources not yet in `hashicorp/aws`. **[Agent: terraform-aws]**
- [x] Cross-check resource shapes against AWS docs via `aws-knowledge-mcp-server` — what's the minimal viable input set for each AgentCore resource, plus the lifecycle behaviour of Memory on destroy (validates tech-spec §3.2 Risk 2 + spec 002 §2.7 acceptance criterion). Append findings to `infra/terraform/RESEARCH.md`. **[Agent: terraform-aws]**
- [x] Create `infra/terraform/` with the standard layout per `terraform-conventions`: `versions.tf` (exact-pinned terraform + `hashicorp/aws` versions per the research), `variables.tf` (region, account, environment, owner, agent_id — no profile name as a variable; rely on default credential chain), `locals.tf` (tag map: `Project=Graphia`, `ManagedBy=Terraform`, plus `Environment` + `Owner` from variables), `outputs.tf` (placeholders for runtime_invocation_url, cloudwatch_log_group, memory_namespace), `main.tf` (empty for now apart from the provider block), and `README.md` with apply / destroy instructions. **[Agent: terraform-aws]**
- [x] Verify the skeleton: `cd infra/terraform && terraform init && terraform validate && terraform plan -var environment=demo -var owner=<your-email>` succeeds. Plan should show zero resources to add (just provider configuration). **[Agent: terraform-aws]**
- [x] **USER:** Manual smoke test — run `aws sso login --profile my-aws-profile`, then from `infra/terraform/` run `terraform init` + `terraform plan` against the SSO profile. Confirm zero errors and zero unintended resources. _(Confirms the credential chain works end-to-end with Terraform.)_

---

## Slice 3: Runtime container image + minimal AgentCore Runtime resource

_The Runtime infrastructure exists in `us-east-1`: an ECR repo, a built-and-pushed minimal container image, and an `aws_bedrockagentcore_runtime` provisioned against the image. The image's entry-point is a stub that returns a single fake response when invoked — no real graph yet. `uv run python -m graphia` still works locally._

- [x] Add `bedrock-agentcore` as a project dependency: `uv add bedrock-agentcore`. **[Agent: python-backend]**
- [x] Create `src/graphia/runtime/__init__.py` and `src/graphia/runtime/__main__.py`: a stub `AgentCoreApp` that wraps a tiny `lambda payload: {"echo": "stub", "received": payload}` callable and calls `.serve()`. This is the placeholder that Slice 4 will replace with the real compiled graph. **[Agent: langgraph-agentic]**
- [x] Create a `Dockerfile` at the project root that packages the project via `uv sync` and runs `python -m graphia.runtime` as the container entry-point. Include explicit Python version pin and minimal layers. **[Agent: python-backend]**
- [x] Add `aws_ecr_repository` and `aws_bedrockagentcore_runtime` resources to `infra/terraform/main.tf`. Runtime resource points at the ECR image URI, has an IAM execution role (created in the same module) with the minimal policies for Bedrock invoke + CloudWatch logs write. Add `outputs.tf` entries for the ECR repo URL and the runtime invocation endpoint. **[Agent: terraform-aws]**
- [x] Document the image build-and-push prerequisite in `infra/terraform/README.md`: the one-liner that builds the image, logs into ECR, tags by git SHA, and pushes. Note that `terraform apply` is no-op if the image hasn't changed (i.e., the deployment is image-driven). **[Agent: terraform-aws]** _(Build tooling wrapped in repo-root `Makefile` per the build-tooling policy — `make build`, `make push`, `make login-ecr` — and README now references those targets rather than raw `podman push` / `docker push` commands.)_
- [x] **USER:** Manual smoke test — run the image build-and-push one-liner, then `terraform apply` from `infra/terraform/`. Confirm: ECR repo exists, image tag matches local git SHA, Runtime resource is `ACTIVE` (check `aws bedrockagentcore get-runtime` or console), the Terraform output names a valid invocation URL. Then run `uv run python -m graphia` locally and confirm a full game still plays end-to-end. _(User-performed; involves real AWS provisioning.)_

### Slice 3 follow-on: post-smoke polish

_Refinements that surfaced from running the Slice 3 smoke test (deploy → destroy → redeploy cycles) and from the user's policy directives during implementation. Not part of the original Slice 3 plan; recorded here because the work was done in the same window and the changes are bundled in the same commit boundary._

- [x] Fix runtime entry-point binding: `app.run(host="0.0.0.0")` in `src/graphia/runtime/__main__.py`. The SDK's `/.dockerenv` auto-detect doesn't fire under Podman, so without the explicit host the container bound to `127.0.0.1:8080` and external probes (including AgentCore health-checks) got "empty reply from server". Runtime-agnostic fix. **[Agent: langgraph-agentic — done inline]**
- [x] Expand the repo-root `Makefile` from "container build" to "project-wide task runner" per the user's directive (saved as `feedback_makefile_build_tooling.md` memory). New targets: `tf-init`, `tf-fmt`, `tf-validate`, `tf-plan`, `tf-ecr-bootstrap`, `tf-apply`, `tf-destroy`, `deploy` (= init + ECR-bootstrap + push + apply), `redeploy` (= push + apply with current SHA), `destroy`. Defaults pull from environment: `OWNER` from `git config user.email`, `TAG` from `git rev-parse --short HEAD`. **[Agent: python-backend — done inline]**
- [x] Add `var.ecr_force_delete` (default `false`) safeguard and wire it through `aws_ecr_repository.runtime`'s `force_delete` attribute + the Makefile's `TF_VARS`. Default-off prevents accidental image purge on routine destroys; override via `make tf-destroy ECR_FORCE_DELETE=true`. Documented two-step procedure (targeted apply to update state, then destroy) in `infra/terraform/README.md` because the AWS provider reads `force_delete` from prior state at destroy time. **[Agent: terraform-aws — done inline]**
- [x] Rewrite `infra/terraform/README.md` to use Makefile targets as the canonical entry points (`make deploy`, `make redeploy`, `make destroy`) instead of multi-step shell sequences. Added `ecr_force_delete` to the Inputs table; added the two-step destroy procedure. **[Agent: terraform-aws — done inline]**
- [x] Update `technical-considerations.md`: §2.5 documents the host-binding rationale; §2.9 confirms resource names (`aws_bedrockagentcore_agent_runtime`, no `invocation_url` attribute), removes the stale `profile` variable, adds the resource-naming convention table, documents the `ecr_force_delete` safeguard and default-tags table; §2.11 notes the CloudWatch log group lands in Slice 3 (Runtime needs a write destination from boot); new §2.12 captures the deployment workflow (Makefile targets, defaults, image-driven deploys, bootstrap-then-apply rationale). **[Agent: langgraph-agentic — done inline]**
- [x] Add `.gitignore` rules for Terraform local working state (`**/.terraform/`, `*.tfstate*`, `*.tfvars`, `*.tfplan`, crash logs, override files); keep `.terraform.lock.hcl` tracked. **[Agent: terraform-aws — done inline]**
- [x] **USER:** Smoke-test the Makefile changes through three tiers — Tier 1 (read-only: `tf-fmt`, `tf-validate`, `tf-plan` shows zero drift), Tier 2 (idempotent against current state: `redeploy` with unchanged SHA = no-op push + no-op apply), Tier 3 (destructive full cycle: `destroy` + `deploy`). Tier 3 surfaced the ECR-not-empty issue which produced the `ecr_force_delete` work above. _(User-performed; all three tiers passed.)_

---

## Slice 4: Local AgentCore client + full remote game (HITL across the wire)

_The headline slice: `uv run python -m graphia --remote` connects to the deployed Runtime, plays a complete Mafia game end-to-end with the human inside the graph, and ends with a decisive winner. The Runtime now hosts the real compiled LangGraph state graph; HITL round-trips work across the wire. No Memory or Gateway yet — diary store is in-process inside the Runtime as a placeholder._

- [x] Replace the stub `src/graphia/runtime/__main__.py` with the real entry-point: build the compiled `StateGraph` (same `build_graph` function as local mode, with `SqliteSaver` writing to `/tmp/graphia/checkpoints/` inside the container's session-local filesystem per tech-spec §2.5). Wrap it in `AgentCoreApp(graph)` and call `.serve()`. **[Agent: langgraph-agentic]** _(Real SDK API: `BedrockAgentCoreApp` + async-generator `@app.entrypoint` + `app.run(host="0.0.0.0")`. New helper `src/graphia/runtime/graph_builder.py` mirrors `build_graph` for caller-supplied thread_id + tmpfs checkpoint dir — duplicated topology rather than mutate `graph.py`; equivalence is the safety net. Payload contract documented inline.)_
- [x] Create `src/graphia/agentcore_client.py`: thin wrapper around `bedrock_agentcore.client.RuntimeClient` constructed from `config.runtime_invocation_url` + default credential chain. Exposes a single `stream(payload, run_config)` method returning an iterator that matches the local-mode `graph.stream(...)` chunk shape. **[Agent: langgraph-agentic]** _(Used `boto3.client('bedrock-agentcore').invoke_agent_runtime` — `bedrock_agentcore.client.RuntimeClient` is spec speculation, doesn't exist in `bedrock-agentcore==1.9.0` (server-side primitives only). SSE-frame-to-local-chunk translation: `{node, update}` → `{node: update}`; `event=interrupt` → `{"__interrupt__": (Interrupt(value, id),)}` (confirmed against langgraph 1.x probe); `event=done` exits iterator; `event=error` raises `AgentCoreRuntimeError`.)_
- [x] Branch `src/graphia/driver.py`'s producer: in remote mode, replace the `graph.stream(...)` call in `_producer` with a call to `agentcore_client.stream(...)`. The `_consume_stream` side is unchanged (it just reads from the `asyncio.Queue`). The resume hand-off (`Command(resume=...)`) routes through the AgentCore client's re-invocation API against the same `thread_id`. **[Agent: langgraph-agentic]** _(In-place branch via tiny `_make_stream_iterator(graph, client, payload, run_config)` helper; `AgentCoreClient` constructed once per `drive_graph` invocation; resume passes the same `Command(resume=value)` to either path. `drive_graph` gained an optional `config=None` parameter — call site in `ui/app.py` updated; default keeps all existing tests green.)_
- [x] Bake a placeholder in-process `DiaryStore` into the Runtime: store diary entries in a small in-Runtime dict keyed by `(game_id, player_id)`. This is the smoke-test placeholder; Slice 6 replaces it with AgentCore Memory. **[Agent: langgraph-agentic]** _(`src/graphia/runtime/diary_store.py` — `InRuntimeDiaryStore` + `DiaryEntry` dataclass; dict-backed with threading.Lock; module-level singleton instantiated in `runtime/__main__.py`. Deliberately unwired into the graph — Slice 6 introduces the `DiaryStore` Protocol + factory + Night-phase call-sites alongside the AgentCore Memory implementation.)_
- [x] Add `tests/test_remote_mode_smoke.py`: end-to-end remote-mode integration test using a *mocked* AgentCore SDK (the `bedrock_agentcore.client.RuntimeClient` is patched at its import boundary, mirroring the spec-001 `safe_llm` fixture). Test drives a complete scripted game; asserts the final state matches expectations and the stream events flowed through the local UI rendering path. **[Agent: testing]** _(`FakeAgentCoreClient` patched at `graphia.driver.AgentCoreClient`; proxies into the captured local graph so chunk shapes are guaranteed identical; 3 new tests — dedicated remote smoke + parametrised local/remote equivalence pair.)_
- [x] Verify automated tests pass: `uv run pytest tests/test_remote_mode_smoke.py -q`, plus the existing spec-001 slice tests still pass (`uv run pytest -q`). **[Agent: testing]** _(36 passed, 22.73s.)_
- [x] Rebuild and push the Runtime image, run `terraform apply` (image SHA changes → Runtime picks up the new image). **[Agent: terraform-aws]** _(Image `:9e1848b` built + pushed via `make push`; `make tf-apply` rolled the Runtime in-place. Note: image tag matches the prior commit SHA but contains Slice 4 code — a small traceability mismatch that resolves on the next commit + redeploy.)_
- [x] **USER:** Manual smoke test — `uv run python -m graphia --remote`. Confirm: the game opens with the Textual UI, plays end-to-end against the hosted Runtime (Night → Day → Night → win), HITL inputs round-trip correctly (the modal pops, your input is accepted, the next super-step runs), and the game reaches a decisive ending. Also run `uv run python -m graphia` (no `--remote`) and confirm local mode is unaffected. _(User-performed; requires real Runtime invocation.)_ _(Confirmed after the Nova switch + Slice 4 driver/client/wire-format fixes deployed. Full --remote game plays end-to-end through the Textual UI; ADR 003 logs the Claude → Nova decision; local mode unaffected.)_

---

## Slice 5: `[local]` / `[remote]` corner badge in Textual UI

_Spec 002 §2.2 acceptance: a persistent corner badge tells the player which mode they're in. Cheap to add now that both modes work; keeps the human safe from "did I really launch --remote?" doubt._

- [x] Add `src/graphia/ui/badge.py`: a `CornerBadge(Widget)` Textual widget with a one-character pad and label text bound at construction time. CSS positions it absolutely in a corner; it doesn't intercept input or steal focus. **[Agent: textual-tui]** _(20-line `Widget` subclass; `DEFAULT_CSS` inline (matches `widgets.py` pattern); top-right corner via `layer: overlay` + `dock: top` + `offset-x: 100%`; `can_focus = False`; default `id="mode-badge"`. Mounting sub-task needs to declare `layers: base overlay;` on the Screen CSS so the overlay layer exists.)_
- [x] Mount the `CornerBadge` in `src/graphia/ui/app.py`: read `GraphiaConfig.remote_mode` at app boot, pass `"[remote]"` or `"[local]"` as the label. Badge visible from the welcome screen through end-of-game. **[Agent: textual-tui]** _(Config loaded in `__init__` so `compose()` can read `remote_mode`; badge yielded as sibling-of-Vertical on `layer: overlay`; Screen CSS extended with `layers: base overlay;`. Label resolved in `compose()`.)_
- [x] Add `tests/test_slice_badge.py`: launch `GraphiaApp` via `App.run_test()` in both modes (`remote_mode=True` and `remote_mode=False` via a config-injection fixture); snapshot-assert the badge label is correct. **[Agent: testing]** _(Parametrised `("local", "[local]")` / `("remote", "[remote]")`; env vars set via monkeypatch BEFORE `GraphiaApp()` since the badge's label is captured at `__init__`-time. Asserts query_one finds the badge, label matches, `can_focus is False`.)_
- [x] Verify automated test passes: `uv run pytest tests/test_slice_badge.py -q`. **[Agent: testing]** _(43 passed, 23.17s — 41 prior + 2 new.)_
- [x] **USER:** Manual smoke test — run `uv run python -m graphia` and `uv run python -m graphia --remote` and confirm the corner shows `[local]` and `[remote]` respectively. The badge doesn't obscure gameplay text. _(User-performed; UI affordance.)_ _(Confirmed after two iterations: corner placement was wrong (`offset-x: 100%` → `dock: right`), then label was invisible because Rich markup parser consumed `[local]` as a style tag (fixed by returning `Text(self._label)` from `render()`). Visibility-level regression test added to `tests/test_slice_badge.py` via `app.export_screenshot()` SVG-text capture.)_

---

## Slice 6: AgentCoreMemoryDiaryStore + Memory resource provisioning

_Diary entries now persist in AgentCore Memory in remote mode. Memory store is provisioned via Terraform; the Runtime's in-process placeholder from Slice 4 is replaced by `AgentCoreMemoryDiaryStore`. Per ADR 002 + tech-spec §2.6, one record per diary entry, namespaced via `(game_id, player_id)` in the record body. `terraform destroy` is expected to remove the records as a downstream consequence of destroying the Memory resource — to be validated in Slice 10._

- [x] Create `src/graphia/diary_store.py`: defines the `DiaryStore` Protocol with `write(game_id, player_id, night_index, content)` and `read(game_id, player_id) -> list[DiaryEntry]`. Provide the `InProcessDiaryStore` (backed by `PlayerState.diary_entries` in `GameState`) and the `AgentCoreMemoryDiaryStore` (wraps `bedrock_agentcore.memory.AgentCoreMemory` per the langgraph-agentcore skill's reference). A factory function selects based on `GraphiaConfig.remote_mode`. **[Agent: langgraph-agentic]** _(Real SDK class is `bedrock_agentcore.memory.MemoryClient` — the skill's `AgentCoreMemory` doesn't exist in 1.9.0. Body encoded as JSON-text in a single ASSISTANT message; `actor_id`=player_id, `session_id`=game_id; `kind="diary_entry"` metadata for server-side filter; sort client-side by `night_index`. `InProcessDiaryStore` stays dict-backed (didn't extend `PlayerState`). New `GraphiaConfig.memory_id` read from `GRAPHIA_MEMORY_ID` env. Slice 4 placeholder `runtime/diary_store.py` deleted; Runtime imports `InProcessDiaryStore` from the new module.)_
- [x] Add an `aws_bedrockagentcore_memory` resource to `infra/terraform/main.tf` keyed by a fixed `agent_id = "graphia-mafia-agent"`. Output the Memory namespace identifier. Update the Runtime's IAM execution role to grant Memory read/write on this resource only. **[Agent: terraform-aws]** _(Memory `graphia_demo_memory`, 90-day expiry, raw events only (no `memory_strategies`). IAM `bedrock-agentcore:CreateEvent` + `ListEvents` scoped to the Memory ARN (skill's `Batch*MemoryRecords` action set is for the wrong surface). `GRAPHIA_MEMORY_ID` plumbed natively via the Runtime's `environment_variables` field — implicit Memory→Runtime ordering, no fallback. Plan: 1 add (Memory) + 2 in-place (IAM, Runtime) + 1 output.)_
- [x] Wire the DiaryStore factory into the Runtime entry-point (`src/graphia/runtime/__main__.py`): in remote mode the factory returns `AgentCoreMemoryDiaryStore`; the agent's diary-write/read code paths use the same `DiaryStore` interface regardless of mode (per ADR 001 — parallel implementations behind one interface). Wire it into the actual Night-phase diary-write site that exists in Phase 2 as a smoke-test placeholder. **[Agent: langgraph-agentic]** _(Write site: `night_close` (runs once per Night cycle, has the full surviving-AI iteration). Threaded via `functools.partial(night_close, diary_store=store, game_id=thread_id)` at graph-build time. Option A — both `build_graph` (local) and `build_runtime_graph` (remote) thread the store, so equivalence holds across modes. Signatures grew optional `diary_store` kwargs with safe defaults; all 45 existing tests stay green.)_
- [x] Add `tests/test_diary_store.py`: parametrised tests exercising the `DiaryStore` Protocol against both implementations. Verify write/read shape, ordering by `night_index`, isolation between `(game_id, player_id)` pairs, append-only semantics. The `AgentCoreMemoryDiaryStore` tests patch the AgentCore SDK at the import boundary (mirroring `safe_llm`). **[Agent: testing]** _(17 new tests: 14 parametrised across both stores (round-trip, sort, empty-read, pair isolation, append-only, return-independence, interleave) + 3 AgentCore-specific (actor/session key mapping, kind filter wire shape, non-diary-event skipping). New autouse `safe_memory_client` in conftest mirrors `safe_llm` — patches `bedrock_agentcore.memory.MemoryClient` (the source-attribute seam, since `_get_client` does a local import). No drift detected between implementations.)_
- [x] Verify automated tests pass: `uv run pytest tests/test_diary_store.py -q`. **[Agent: testing]** _(62 passed, 24.29s — 45 prior + 17 new.)_
- [x] Rebuild Runtime image (now includes the AgentCore Memory client) and `terraform apply`. **[Agent: terraform-aws]** _(Two redeploys: first with `make_diary_store` gated on `remote_mode` (writes silently landed in InProcess inside the container — see Slice 6 follow-on commit), then re-fixed to gate on `memory_id` and redeployed. Final image SHA on the Runtime is `eb27ffa`.)_
- [x] **USER:** Manual smoke test — play a complete `--remote` game. After the game ends, use `aws bedrock-agentcore-memory list-memories --agent-id graphia-mafia-agent` (or the appropriate CLI per the Slice 2 research) to confirm diary records exist in Memory, one per Night-phase diary write. Confirm the records carry the expected `(game_id, player_id, night_index, content)` shape. _(User-performed; involves AWS-state inspection.)_ _(Verified via `make inspect-diary` (new utility under `src/graphia/tools/`): 10 entries on the first post-fix game — 5 surviving AIs × 2 Night cycles. Shape `(night_index, player_id, game_id, content)` matches the spec 002 §2.6 schema. The CLI sub-command the spec named (`bedrock-agentcore-memory list-memories ...`) doesn't exist in the actual AWS surface; the correct path is `list-actors`→`list-sessions`→`list-events` per the `MemoryClient` API, which `inspect-diary` wraps.)_

---

## Slice 7: Gateway with runtime-embedded tool targets (per ADR 002)

_The Gateway-fronted MCP surface from spec 002 §2.4 + ADR 002: the Runtime exposes HTTP endpoints `POST /tools/diary/write` and `POST /tools/diary/read`; AgentCore Gateway registers them as `api`-type MCP targets. The agent inside the Runtime now invokes diary tools through Gateway-MCP instead of calling `AgentCoreMemoryDiaryStore` directly. Per ADR 002, both target endpoints route back into the same Runtime container — Gateway is the MCP envelope + audit point, not a separate compute layer._

- [x] Add the HTTP surface to `src/graphia/runtime/__main__.py`: alongside the `AgentCoreApp(graph).serve()` call, mount a tiny ASGI app (FastAPI or `bedrock_agentcore`'s built-in routing if supported) that exposes `POST /tools/diary/write` and `POST /tools/diary/read`. Both handlers delegate to the `AgentCoreMemoryDiaryStore` from Slice 6. **[Agent: langgraph-agentic]** _(Used Starlette's public `app.add_route` — `BedrockAgentCoreApp` extends `Starlette`, so no private attrs needed and `app.run()` stays untouched. Pydantic body validation; shared module-level `_diary_store` singleton. Container smoke confirmed `POST /tools/diary/write` + `/read` round-trip end-to-end alongside the existing `/ping` and `/invocations` surfaces.)_
- [x] Add `aws_bedrockagentcore_gateway` + two `aws_bedrockagentcore_gateway_target` resources to `infra/terraform/main.tf`, each pointing at the Runtime's HTTP base URL + the relevant tool path. Document the chosen target_type (`api`) and the OpenAPI / MCP schema declarations the Gateway needs for tool registration. **[Agent: terraform-aws]** _(Gateway inbound auth: `AWS_IAM` (no Cognito ceremony for a single-developer footprint). Target type is `mcp_server` (NOT `api` — corrects RESEARCH.md §6 Q5); endpoint resolves to `https://bedrock-agentcore.us-east-1.amazonaws.com/runtimes/<arn>/invocations/mcp`. Plan: 5 add (Gateway, 2 targets, Gateway IAM role + policy) + 2 in-place (Runtime IAM grants `InvokeGateway`; Runtime env gets `GRAPHIA_GATEWAY_ID`) + 3 new outputs. Tools list is pulled implicitly by Gateway via `tools/list` on `CreateGatewayTarget` — so apply needs an already-running MCP-speaking Runtime image.)_
- [x] Refactor the agent-side diary read/write in the Runtime to call the Gateway-published MCP tools instead of the `AgentCoreMemoryDiaryStore` directly. The `AgentCoreMemoryDiaryStore` becomes the *underlying* implementation (called via the HTTP handler); the *agent* calls `gateway_client.invoke("diary_write", ...)`. Local mode remains direct (no Gateway). **[Agent: langgraph-agentic]** _(Scope expanded by Slice 7 sub-task 2's finding that Gateway only supports `mcp_server` targets — Starlette routes from sub-task 1 replaced with FastMCP server at `/mcp` mounted on the existing `BedrockAgentCoreApp`. Two-store split in the Runtime: `_impl_diary_store` (`AgentCoreMemoryDiaryStore`) backs the MCP tool handlers; `_agent_diary_store` (`GatewayMCPDiaryStore`) is what the agent's `night_close` writes through (Runtime → Gateway → Runtime same-container). `GatewayMCPDiaryStore` SigV4-signs MCP `streamablehttp_client` requests via a small `httpx.Auth` shim + `botocore.auth.SigV4Auth`. New deps: `mcp>=1.27.1`. Terraform: `protocol_configuration { server_protocol = "MCP" }` added to the Runtime resource. `safe_gateway_mcp_client` autouse in conftest mirrors `safe_memory_client`.)_
- [x] Add `tests/test_gateway_mcp_smoke.py`: assert the runtime-embedded HTTP surface accepts well-formed payloads, rejects malformed ones, and round-trips a `(game_id, player_id, night_index, content)` tuple through a *mocked* AgentCore Memory backend. **[Agent: testing]** _(19 tests across three layers: 4 `_parse_mcp_tool_result` unit tests (modern `structuredContent` vs legacy `TextContent` shapes — pins the MCP version-skew invariant), 4 in-process FastMCP server smoke (`tool-manager.call_tool` direct, no HTTP) confirming `diary_write` / `diary_read` delegate into `_impl_diary_store`, 11 `GatewayMCPDiaryStore` client tests with both `streamablehttp_client` and `ClientSession` mocked plus 1 real SigV4 signature assertion. `pytest-asyncio` already present.)_
- [x] Verify automated tests pass: `uv run pytest tests/test_gateway_mcp_smoke.py -q`. **[Agent: testing]** _(81 passed in 24.23s; 62 prior + 19 new.)_
- [x] Rebuild Runtime image and `terraform apply`. **[Agent: terraform-aws]**
- [x] **USER:** Manual smoke test — play a complete `--remote` game. Confirm diary writes still land in Memory (`aws bedrock-agentcore-memory list-memories ...`). Confirm via CloudWatch (or `aws bedrock-agentcore get-gateway-target ...`) that Gateway tool invocations are visible — i.e., the agent really did call through Gateway, not bypass it. _(User-performed.)_ _(Verified after the ADR-005 Lambda pivot + the bug-fix chain in the follow-on below. A full `--remote` game plays end-to-end through the agent → Gateway → Lambda → Memory chain; `make inspect-diary` shows entries; private Moderator whispers render correctly post the remote-mode UI-state fix.)_

### Slice 7 follow-on: ADR-005 Lambda pivot + remote-deploy bug chain

_Slice 7's runtime-embedded design (ADR 002) collided with an AgentCore constraint that only surfaced at deploy time, forcing a re-architecture (ADR 005) and a chain of remote-Runtime-only fixes. Recorded here because the work was substantial and bundled across the same window — none of it was in the original Slice 7 plan._

- [x] **ADR 004** — logged the `mcp_server` Gateway-target IAM-credential-provider provider gap (`hashicorp/aws 6.44.0` can't express `IamCredentialProvider`) and the original CLI-workaround. _(Superseded in practice by ADR 005 — Lambda targets have no such gap.)_
- [x] **ADR 005 supersedes ADR 002** — AgentCore Runtime's `protocol_configuration` is mutually exclusive (`HTTP` agent-stream vs `MCP` tool-server); one Runtime can't host both. Pivoted from runtime-embedded FastMCP handlers to **Lambda-target Gateway tools** (the canonical AgentCore pattern). ADR 002 status flipped to Superseded. **[langgraph-agentic + terraform-aws]**
- [x] Gut the FastMCP server from `src/graphia/runtime/__main__.py`; Runtime reverts to HTTP protocol (Slice 4 agent-stream restored). `make_diary_store` factory re-ordered (`gateway_url` → `memory_id` → InProcess). **[langgraph-agentic]**
- [x] Two zip-deployed Lambda functions (`graphia-demo-diary-write` / `-read`) under `infra/lambda/`, each packaging the `AgentCoreMemoryDiaryStore` logic; Terraform `aws_lambda_function` + shared IAM role + per-fn CloudWatch log groups; Gateway targets re-typed `mcp_server` → `lambda` (provider-supported, no gap); Runtime IAM loses direct Memory grants. **[terraform-aws]**
- [x] Fix: `asyncio.run()` cannot run inside a running event loop — `GatewayMCPDiaryStore._run_sync` bridges via a fresh loop in a `ThreadPoolExecutor` when a loop is already running. **[langgraph-agentic — done inline]**
- [x] Fix: `mcp 1.27+` `streamablehttp_client(auth=...)` does an `isinstance(auth, httpx.Auth)` check — `_SigV4HttpxAuth` now subclasses `httpx.Auth`. **[done inline]**
- [x] Fix: Gateway namespaces tools as `<target>___<tool>` — client tool names corrected to `graphia-diary-write___diary_write` / `graphia-diary-read___diary_read` (verified via a live `tools/list`). **[done inline]**
- [x] Fix: `make build-lambdas` pip-installed host (macOS/arm64) wheels — Lambda failed at import (`pydantic_core._pydantic_core`). Build now forces `--platform manylinux2014_x86_64 --only-binary=:all:` Linux wheels. **[done inline]**
- [x] Fix: three UI methods (`_refresh_human_id`, `_check_spectator_transition`, `_current_cycle`) read the local graph's `get_state` — empty in remote mode, so private whispers were buffered forever, spectator transition + per-Day vote hint were dead. Driver gained an `on_state` callback; the UI mirrors stream state into `_latest_state`. Same fix class as the Slice 4 stream-captured-interrupt change. **[langgraph-agentic — done inline]**
- [x] Tooling: `make build-lambdas` / `clean-lambdas` added; `make gateway-auth` added then removed (ADR 004 workaround obsolete under ADR 005). Tests churned: FastMCP-server-side tests deleted, Lambda/client/regression tests added — **83 passing**.

---

## Slice 8: AgentCore Observability + 30-day retention + failure modal

_Spec 002 §2.5: structured traces in CloudWatch for every remote-mode session, 30-day log retention, and a Textual failure modal pointing the player at the CloudWatch log group + filter when a remote game crashes. Per the tech spec's design decision: AgentCore Observability provides the agent/tool/Memory traces; LangSmith is opt-in only._

- [x] Add `aws_cloudwatch_log_group` to `infra/terraform/main.tf` with `retention_in_days = 30`. Enable AgentCore Observability on the Runtime resource (whichever Terraform field surfaces this — confirmed in Slice 2 research). Add the log group ARN as a Terraform output. **[Agent: terraform-aws]** _(30-day log group already existed from Slice 3 — reused, no duplicate. AgentCore Observability has no argument on `aws_bedrockagentcore_agent_runtime` in `hashicorp/aws 6.44.0`; the Runtime-scoped half is implemented as a 6-resource CloudWatch vended-log-delivery pipeline (`aws_cloudwatch_log_delivery_source`/`_destination`/`_delivery` × APPLICATION_LOGS + TRACES). The account-level CloudWatch Transaction Search step (`UpdateTraceSegmentDestination`) has no provider surface — documented as a gap in `RESEARCH.md` §12 with a one-time `aws` CLI workaround for the apply sub-task. New output `cloudwatch_log_group_arn`. `terraform fmt`/`validate` pass; `plan` deferred to the apply sub-task.)_
- [x] Wire the Runtime to emit a per-session correlation id (the LangGraph `thread_id`) into every trace event so a single game's events are filterable. **[Agent: langgraph-agentic]** _(New `src/graphia/runtime/observability.py`: a `contextvars.ContextVar` holds the invocation's `thread_id`; a `logging.Filter` injects it onto every `LogRecord` and a `logging.Formatter` renders each record as single-line JSON with `thread_id` as a top-level key — the exact shape a CloudWatch `{ $.thread_id = "..." }` filter matches. `runtime/__main__.py` calls `bind_thread_id` + `stamp_trace_thread_id` as the first work in the `@app.entrypoint` handler and emits structured lifecycle events (invocation_start / graph_step / invocation_done|interrupt / error-with-traceback). The image runs `python -m graphia.runtime` without an OTEL wrapper, so AgentCore captures the field via the container's stdout `APPLICATION_LOGS` stream (routed by the prior task's vended-log-delivery pipeline); `stamp_trace_thread_id` is a soft-imported best-effort OTEL span/baggage stamp that lights up if a future deploy adds ADOT. Local mode untouched. 8 new tests; suite 83 → 91 green.)_
- [x] Add a `src/graphia/ui/failure_modal.py`: a Textual modal screen shown when the local app catches an unhandled exception during `--remote` play. The modal text includes the CloudWatch log group + a copy-pasteable filter expression (`{ $.thread_id = "<thread>" }`) for the failed session. Local-mode crash behaviour is unchanged (existing banner + log file pointer). **[Agent: textual-tui]** _(`FailureModal(ModalScreen[None])` — inline `DEFAULT_CSS` per the `badge.py` convention; constructor takes `thread_id` + optional `log_group` + `error_summary`; dismissible via button or `escape`/`enter`. `GraphiaApp._drive`'s crash `except` now branches on `config.remote_mode`: remote → one-line banner + `_show_failure_modal`; local → unchanged banner + JSONL pointer. `thread_id` reaches the modal via a new `self._thread_id` set from `build_graph`'s return (falls back to `"<unknown>"` if the crash precedes it). New `GraphiaConfig.cloudwatch_log_group` read from `GRAPHIA_LOG_GROUP`; `make wire-env` now pulls the `cloudwatch_log_group` Terraform output into `.env` as a third awk branch. Graceful degradation: when the log group is `None` the modal shows `(unknown — run terraform output …)` plus the still-valid filter, never `"None"`. Suite green at 91.)_
- [x] Add `tests/test_slice_failure_modal.py`: in remote-mode test setup, inject an exception during stream consumption; assert the failure modal renders with the expected text including the log group identifier and a thread-id filter expression. **[Agent: testing]** _(3 tests: (1) remote crash shows a `FailureModal` carrying the configured log-group name + the `{ $.thread_id = "<thread>" }` filter with the real session id; (2) modal degrades to `(unknown — run terraform output …)` when `GRAPHIA_LOG_GROUP` is unset; (3) local-mode crash shows no modal — regression guard on the `remote_mode` branch. Exception seam: a `CrashingAgentCoreClient` monkeypatched onto `graphia.driver.AgentCoreClient` whose `stream()` raises lazily on first iteration — an unhandled error during stream consumption that propagates to the real `GraphiaApp._drive` try/except, after `build_graph` has returned a real `thread_id`. No production code modified. Full suite 91 → 94.)_
- [x] Verify automated tests pass: `uv run pytest tests/test_slice_failure_modal.py -q`. **[Agent: testing]** _(3 passed in 0.61s.)_
- [x] Rebuild Runtime image and `terraform apply`. **[Agent: terraform-aws]** _(Image `:c77d4f9` deployed (Slice 8 observability) then `:4f164f3` (CR 003 OTEL instrumentation). Apply rolled the Runtime in-place + added the 6 vended-log-delivery resources. USER-verified: a `--remote` game's structured `thread_id`-correlated logs land in the CloudWatch log group.)_
- [ ] **USER:** Manual smoke test — play a `--remote` game to completion, then open the CloudWatch log group whose name is in the Terraform output. Confirm structured trace events are present, the `thread_id` correlation id appears, and the log group has `retention_in_days = 30`. Then deliberately misconfigure (e.g., point `GRAPHIA_RUNTIME_URL` at a non-existent URL) to trigger the failure modal; confirm it renders with a valid CloudWatch link/filter. _(User-performed; mixed AWS state + UI smoke.)_

### Slice 8 follow-on: OpenTelemetry instrumentation for navigable trace trees (CR 003)

_[CR 003](../../change-requests/003-observability-navigable-trace-trees.md): the Slice 8 deployment smoke test showed AgentCore Observability delivers flat, session-correlated application logs but no navigable span tree — the Runtime container is not OpenTelemetry-instrumented. This subsection adds the instrumentation so the GenAI Observability console renders a per-session trace tree._

- [x] Instrument the deployed Runtime with OpenTelemetry (ADOT) so AgentCore Observability renders a navigable per-session trace tree — graph invocation, per-turn model calls, Gateway tool calls — correlated by `thread_id` (activates `observability.py`'s `stamp_trace_thread_id`). Local mode unaffected; test suite stays green. **[Agent: langgraph-agentic]** _(Added `aws-opentelemetry-distro` (0.17.0); Dockerfile `CMD` runs the Runtime under `opentelemetry-instrument` with `OTEL_*` env (AWS distro/configurator, `service.name=graphia-runtime`). ADOT's bundled instrumentors — `aws_langchain`, `botocore`, `aws_mcp`, `httpx` — cover LangGraph nodes, Bedrock calls, and Gateway MCP calls. `stamp_trace_thread_id` rewritten to put `thread_id` into OTEL baggage under `session.id` (the key GenAI Observability groups a session by) + as a span attribute. Local mode never loads the ADOT wrapper. Suite 94 → 95. Committed `4f164f3`, deployed.)_
- [ ] Rebuild Runtime image and `terraform apply`; confirm the trace tree appears in the AgentCore GenAI Observability console for a `--remote` game. Sync the CR 003 shift into `functional-spec.md` §2.5, `technical-considerations.md` §2.11, and `roadmap.md` Phase 2 wording. **[Agent: terraform-aws]**

---

## Slice 9: Equivalence tests + end-to-end smoke

_Per tech-spec §4 and Risk "Two parallel DiaryStore impls drift out of sync semantically": exercise `InProcessDiaryStore` and `AgentCoreMemoryDiaryStore` against the same scripted scenarios so behavioural drift surfaces in CI rather than in user-facing remote play._

- [ ] Extend `tests/test_diary_store.py` (from Slice 6) with explicit equivalence-parametrisation: a single set of scenarios (write three entries, read them back, read with no entries, read for a different player_id) runs against both implementations and asserts identical observable behaviour. **[Agent: testing]**
- [ ] Add `tests/test_dual_mode_smoke.py`: drive a complete game once in local mode (real `InProcessDiaryStore`) and once in remote mode (mocked AgentCore SDK + `AgentCoreMemoryDiaryStore`) using the same scripted human inputs. Assert the public log messages, kill log, and final winner match exactly. **[Agent: testing]**
- [ ] Verify the existing full suite still passes: `uv run pytest -q`. Spec-001 slice tests must remain green. **[Agent: testing]**
- [ ] **USER:** Manual smoke test — none required for this slice; the CI suite is the verification surface. _(Optional: play one game in each mode for sanity, but CI covers the equivalence claim.)_

---

## Slice 10: `terraform destroy` cleanup verification + deploy/destroy README

_Spec 002 §2.7: `terraform destroy` removes every resource provisioned in Slices 2–8, including AgentCore Memory data; re-applying from clean state succeeds without naming conflicts. Document the full deploy/destroy lifecycle as a checklist a fresh contributor can follow._

- [ ] Expand `infra/terraform/README.md`: pre-flight checklist (SSO login, environment + owner variables), apply procedure (build image, push to ECR, `terraform apply`), destroy procedure (`terraform destroy`, expected Memory-data behaviour, manual cleanup steps if any), troubleshooting (common AgentCore + Bedrock errors and their resolutions). **[Agent: terraform-aws]**
- [ ] Run `terraform destroy` against the live deployment from a clean state. Use `aws-api-mcp-server` (or the AWS CLI directly) to verify post-destroy: (a) Runtime resource is gone (`aws bedrockagentcore list-runtimes`), (b) Gateway is gone (`aws bedrockagentcore list-gateways`), (c) Memory records for `agent_id=graphia-mafia-agent` are gone, (d) the CloudWatch log group is gone (or in `PENDING_DELETION`). Document any unexpected residual resources as known limitations in `RESEARCH.md`. **[Agent: terraform-aws]**
- [ ] Re-run `terraform apply` from the now-clean state and confirm a successful re-deploy: no name conflicts, all four AgentCore services come back up, the same `terraform output` shape is produced. Rebuild the Runtime image (or reuse the existing ECR tag if it survives the destroy — note which behaviour applies). **[Agent: terraform-aws]**
- [ ] **USER:** Manual smoke test — play one complete `--remote` game against the re-deployed Runtime. Confirm gameplay works end-to-end, diaries appear in the new Memory store, traces flow to the new CloudWatch log group. Then `terraform destroy` one more time as a final cleanup if you don't intend to keep the deployment running. _(Final user-performed validation of the whole Phase 2 deliverable.)_

---

## Coverage / agent assignment summary

All sub-tasks above are assigned to specialist subagents (no `general-purpose` fallback used). MCPs required for verification:

| MCP / Tool | Used in | Purpose |
|---|---|---|
| `terraform-mcp-server` | Slice 2 | Confirm AgentCore Terraform resource names from the Registry |
| `aws-knowledge-mcp-server` | Slice 2 | Validate Memory destroy-lifecycle and AgentCore service-plane behaviour |
| `aws-api-mcp-server` | Slices 6, 7, 8, 10 | Inspect live AWS state during user smoke tests + Slice 10 cleanup verification |
| `langgraph-agentcore` skill | Slices 3, 4, 6, 7, 8 | AgentCore Runtime entry-point, Memory record shape, Gateway target shape, Observability wiring |
| `terraform-conventions` skill | Slices 2, 3, 6, 7, 8 | the configured IaC house style (version pinning, required tags, file layout) |

All MCPs and skills are already installed (via the `/awos:hire` run earlier). No missing capabilities; no installation prerequisites.