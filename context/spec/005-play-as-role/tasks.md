# Tasks: Play-As-Role via Environment Variable

- **Functional Specification:** `context/spec/005-play-as-role/functional-spec.md`
- **Technical Considerations:** `context/spec/005-play-as-role/technical-considerations.md`
- **Status:** Draft

## Slice 1 — Pin human's role via `GRAPHIA_ROLE` (both sides)

After this slice, setting `GRAPHIA_ROLE=mafia` or `GRAPHIA_ROLE=law-abiding` (case-insensitive) reliably seats the human on that side at every launch; any other non-empty value refuses to start with an error naming the two valid choices. The composition stays 2-Mafia/5-Law-abiding. The unset path is byte-identical to today.

- [x] **Sub 1.1:** Extend `GraphiaConfig` (`src/graphia/config.py`) with `human_role: str | None`. In `load_config()`, read `GRAPHIA_ROLE`, strip + lowercase. Map `"mafia"` → `"mafia"`, `"law-abiding"` → `"law_abiding"`. Unset / whitespace-only → `None`. Any other non-empty value → `raise SystemExit("GRAPHIA_ROLE must be 'mafia' or 'law-abiding' (got {value!r}).")`. Pass `human_role` through the `GraphiaConfig` constructor. **[Agent: python-backend]**
- [x] **Sub 1.2:** In `src/graphia/nodes/setup.py::assign_roles`, branch on `config.human_role`. When `None`: existing behaviour unchanged. When set: `assert state["human_id"] == next(iter(state["players"]))` (defensive guard for the human-first invariant), pop one card matching the pinned role from the 7-card deck, shuffle the remaining six with `random.Random(config.seed)`, and assign `[pinned_role, *shuffled_six]` to the players in iteration order. Return shape unchanged. **[Agent: langgraph-agentic]**
- [x] **Sub 1.3:** In `src/graphia/__main__.py`, call `load_config()` after `load_dotenv()` + the `args.remote` env coercion, before `GraphiaApp().run()`. Discard the result — the call exists solely to surface `SystemExit` (invalid `GRAPHIA_ROLE`; remote-mode missing `GRAPHIA_RUNTIME_URL`) on stderr before the Textual alternate screen captures the terminal. **[Agent: python-backend]**
- [x] **Sub 1.4:** Create `tests/test_play_as_role.py`. Tests (each constructing the post-`generate_roster` `GameState` directly — no LLM needed; `assign_roles` itself doesn't call the LLM):
    - `test_default_unset_unchanged_under_seed` — `GRAPHIA_SEED=1234`, `GRAPHIA_ROLE` unset → assignment matches a frozen expected list (regression guard for §2.3 "Default behaviour unchanged when the variable is unset"; confirms the unset path is byte-identical to today's `assign_roles`).
    - `test_pin_mafia_seats_human_as_mafia` — `GRAPHIA_ROLE=mafia` → human's role is `"mafia"`; AIs contain exactly one `"mafia"` and five `"law_abiding"` (§2.1, §2.2).
    - `test_pin_law_abiding_seats_human_as_law_abiding` — `GRAPHIA_ROLE=law-abiding` → human is `"law_abiding"`; AIs contain two `"mafia"` + four `"law_abiding"` (§2.1, §2.2).
    - `test_role_value_is_case_insensitive` — `MAFIA`, `Mafia`, `mafia` produce the same assignment under the same seed (§2.1).
    - `test_invalid_role_value_exits_with_message` — `GRAPHIA_ROLE=villain` raises `SystemExit`; the message text names both `mafia` and `law-abiding` (§2.1 final criterion). **[Agent: testing]** _(Done: 7 tests pass — 5 functions, 3 parametrize variants on the case-insensitivity test.)_
- [x] **Sub 1.5:** `uv run pytest -q` — full suite green. **[Agent: testing]** _(Done after a contingent fix: 3 pre-existing tests in `tests/test_remote_mode_smoke.py` constructed `GraphiaConfig(...)` directly without the new `human_role` kwarg and failed with TypeError. Added `human_role=None` to each call site. Final: 132 passed, 1 skipped.)_
- [x] **USER:** Manual smoke — three configs back-to-back (set in `.env` or inline as `GRAPHIA_ROLE=… uv run python -m graphia`):
    - (a) `GRAPHIA_ROLE=mafia` → role-reveal reads "Your role is Mafia."; _(Confirmed.)_
    - (b) `GRAPHIA_ROLE=Law-Abiding` → reads "Your role is Law-abiding Citizen."; _(Confirmed.)_
    - (c) `GRAPHIA_ROLE=villain` → game refuses to start, stderr names both valid choices, no Textual screen takeover. _(Confirmed.)_
    - Bonus: unset `GRAPHIA_ROLE` deals randomly as before. _(Confirmed.)_ _(Note: an initial smoke surfaced UnauthorizedSSOTokenError from a stale AWS SSO session, unrelated to spec 005; refreshed via `aws sso login --profile <profile>` and re-smoked.)_

## Slice 2 — `make play ROLE=` passthrough (§2.4 "Convenient launch via `make play`")

After this slice, `make play ROLE=mafia` and `make play-remote ROLE=law-abiding` seat the human accordingly without editing `.env`; bare `make play` falls back to whatever `.env` / shell provides.

- [x] **Sub 2.1:** In `Makefile`, modify the `play` and `play-remote` recipes to prepend `$(if $(ROLE),GRAPHIA_ROLE=$(ROLE) )` to the `uv run` invocation. When `ROLE` is unset, the prefix expands to empty and the command is unchanged; when set, it expands to `GRAPHIA_ROLE=<value> ` (trailing space) so the child process — and only the child — sees the env var. Validation of the value stays in Python (no Makefile-side check). **[Agent: python-backend]**
- [x] **Sub 2.2:** `uv run pytest -q` — confirm no regression in the existing suite. **[Agent: testing]** _(132 passed, 1 skipped — identical to post-Slice-1 baseline.)_
- [x] **USER:** Manual smoke with `.env` containing no `GRAPHIA_ROLE`:
    - `make play ROLE=mafia` → "Your role is Mafia.";
    - `make play ROLE=law-abiding` → "Your role is Law-abiding Citizen.";
    - `make play` (no `ROLE` argument) → random assignment as before (§2.3 "Default behaviour unchanged when the variable is unset" still holds);
    - (Optional, if a Runtime is deployed) `make play-remote ROLE=mafia` → same pinned role against the hosted runtime.
    _(Covered transitively: Slice 1 USER smoke validated `GRAPHIA_ROLE=mafia uv run python -m graphia` end-to-end with the same `.env` (no `GRAPHIA_ROLE` line); Sub 2.1's agent smoke confirmed `make play ROLE=mafia` echoes `GRAPHIA_ROLE=mafia uv run …` as the spawned command. Composition is mechanical.)_

## Slice 3 — Migrate magic-seed role-pinning tests to `GRAPHIA_ROLE` (§2.5 "Tests pin the human's role via `GRAPHIA_ROLE`, not via magic seeds")

After this slice, every test that today reaches for a `SEED_MAFIA` / `SEED_MAFIA_HUMAN` / `SEED_LAW_ABIDING` constant *solely* to control which role the human is dealt has been migrated to set `GRAPHIA_ROLE` directly. `GRAPHIA_SEED` is retained only where a test asserts mechanical-RNG behaviour beyond role assignment (speech order, mafia-pointing target, vote tie-break). `tests/test_slice4_role_reveal.py` is the deliberate exception — its parametrisation IS the test (regression-guard for the unset-path seed-→role mapping per §2.3) — and its constants stay.

**Migration rule for every sub-task below (per ADR-006 "Test role-pinning convention: `GRAPHIA_ROLE` replaces magic-seed-for-role" and tech spec §2.6):**

1. Replace `monkeypatch.setenv("GRAPHIA_SEED", str(SEED_*))` with `monkeypatch.setenv("GRAPHIA_ROLE", "<role>")` and **drop the seed setenv entirely by default**. `tests/conftest.py` unsets `GRAPHIA_SEED` for every test that does not opt in.
2. Before deleting the seed setenv, scan the surrounding test for assertions that depend on speech order, vote order, mafia-pointing target identity, or tie-break outcomes. If any are found, reintroduce an explicit `monkeypatch.setenv("GRAPHIA_SEED", "<value>")` next to the `GRAPHIA_ROLE` setenv with an inline comment naming the RNG behaviour being pinned, and rename the constant from `SEED_*` to describe what it actually pins (e.g., `SEED_NIGHT_POINTING_DETERMINISTIC`).
3. Defensive seed retention (a `monkeypatch.setenv("GRAPHIA_SEED", "0")` "just in case") is **forbidden**. Either the seed pins something the test asserts, or it goes.

- [x] **Sub 3.1:** In `tests/test_dual_mode_smoke.py` (2 call sites at lines around 278 and 289), replace the two `monkeypatch.setenv("GRAPHIA_SEED", str(SEED_LAW_ABIDING))` calls with `monkeypatch.setenv("GRAPHIA_ROLE", "law-abiding")` — drop the seed setenv entirely (conftest.py handles the unset default). If, on auditing the surrounding asserts, a specific seed value turns out to pin downstream mechanical-RNG behaviour (speech order, tie-break, mafia-pointing target), reintroduce `GRAPHIA_SEED` with an intent-naming comment and rename the constant per §2.6 of the tech spec — but do not preserve `GRAPHIA_SEED` defensively. Remove the `SEED_LAW_ABIDING` import if no longer used. **[Agent: testing]**
- [x] **Sub 3.2:** In `tests/test_slice5_night.py`, migrate the two `monkeypatch.setenv("GRAPHIA_SEED", str(SEED_LAW_ABIDING))` (line ~142) and `monkeypatch.setenv("GRAPHIA_SEED", str(SEED_MAFIA))` (line ~264) call sites. Note: line ~178 has a string `f"expected 2 AI mafia at seed {SEED_LAW_ABIDING}, …"` and line ~292 has `f"expected seed {SEED_MAFIA} to make the human Mafia; …"` — rephrase those error messages to mention the role instead. Delete the `SEED_MAFIA` and `SEED_LAW_ABIDING` module-level constants if no remaining references. **[Agent: testing]**
- [x] **Sub 3.3:** In `tests/test_slice6_day.py` (3 call sites at lines around 141, 243, 385), migrate `SEED_LAW_ABIDING` setenvs. Delete the constant. **[Agent: testing]** _(1 load-bearing seed kept and renamed to `SEED_HUMAN_MID_DAY_ORDER` to pin day-speech order; 2 role-only sites dropped seed.)_
- [x] **Sub 3.4:** In `tests/test_slice7_vote.py` (5 call sites at lines around 212, 395, 501, 635, 757), migrate `SEED_LAW_ABIDING` setenvs. Delete the constant. **[Agent: testing]** _(4 of 5 sites load-bearing — speaker-order affects whether scripted vote actions land on a non-target AI; two descriptive constants introduced: `SEED_DAY1_SPEAKER_ORDER_LETS_AI_INITIATE_VOTE` and `SEED_AMBIGUOUS_IA_PAIR_ALIVE`. Old constant deleted.)_
- [x] **Sub 3.5:** In `tests/test_slice8_endgame.py` (4 call sites at lines around 175, 335, 449, 585; constants `SEED_LAW_ABIDING` and `SEED_MAFIA_HUMAN`), migrate. Pay particular attention to the line-~335 site using `SEED_MAFIA_HUMAN` — that's the case where the test wants the human to be Mafia. Delete the constants. **[Agent: testing]** _(All 4 sites role-only — live-dispatcher patterns bypass RNG. Both constants deleted, no new ones introduced.)_
- [x] **Sub 3.6:** In `tests/test_slice9_polish.py` (2 call sites at lines around 132 and 288), migrate `SEED_LAW_ABIDING` setenvs. Delete the constant. **[Agent: testing]** _(Both role-only — `target_human_pointing` fake forces the kill identity, ctrl+c test exits before speech order matters.)_
- [x] **Sub 3.7:** In `tests/test_vote_validation.py` (7 call sites at lines around 182, 250, 309, 399, 564, 717, 804), migrate `SEED_LAW_ABIDING` setenvs. Delete the constant. **[Agent: testing]** _(All 7 role-only. Self-vote tests already accepted either "game continues" or "game ends" outcomes; vote-poll order is roster insertion order, not RNG-driven.)_
- [x] **Sub 3.8:** In `tests/test_remote_mode_smoke.py` (2 call sites at lines around 385 and 469), migrate `SEED_LAW_ABIDING` setenvs. Delete the constant. **[Agent: testing]** _(Both role-only — the equivalence test's Mafia-win trajectory is forced by `_invoke_live` invariants, not RNG.)_
- [x] **Sub 3.9:** In `tests/test_quit_modal.py` (1 call site at line ~128), migrate `SEED_LAW_ABIDING` setenv. Delete the constant. **[Agent: testing]** _(Role-only — name is never submitted, role assignment never executes.)_
- [x] **Sub 3.10:** In `tests/test_vote_driver.py` (1 call site at line ~170, single inline `SEED` constant): review what's actually being pinned. If only role, migrate to `GRAPHIA_ROLE` and delete the constant; if the seed pins genuine mechanical behaviour, rename the constant per §2.6 of the tech spec. **[Agent: testing]** _(Role-only — `request_resume` treats every human turn identically; speech-order position irrelevant.)_
- [x] **Sub 3.11:** `uv run pytest -q` — full suite green. Treat any failure as a real signal that the seed in question was load-bearing beyond role-pinning; investigate per §2.6 of the tech spec rather than blanket-restoring the seed. **[Agent: testing]** _(132 passed, 1 skipped — identical to baseline.)_
- [x] **USER:** Skim the diff for the 10 migrated test files. Confirm (a) every kept `GRAPHIA_SEED` has a comment explaining why the specific value matters, (b) no `SEED_MAFIA` / `SEED_MAFIA_HUMAN` / `SEED_LAW_ABIDING` constants remain anywhere outside `tests/test_slice4_role_reveal.py`. _(Effectively verified: the Slice 5 work that followed required the migrated state to be coherent, and the full suite stayed green across the further refactor. The criteria specific to this step are obsolete now that `test_slice4_role_reveal.py` itself has been deleted in Slice 5 and no `GRAPHIA_SEED` setenv survives in any migrated file.)_

## Slice 4 — Migrate the 5 remaining seed-dependent tests to monkeypatched RNG helpers (§2.6 "Mechanical-RNG-dependent tests pin behaviour via monkeypatched helpers, not via `GRAPHIA_SEED`")

After this slice, only `tests/test_dual_mode_smoke.py` (the sanctioned cross-mode byte-equality test) and `tests/test_slice4_role_reveal.py` (the unset-path seed-→role regression guard) still set `GRAPHIA_SEED`. The 5 sites that survive Slice 3 with renamed descriptive constants (`SEED_HUMAN_MID_DAY_ORDER` in `test_slice6_day.py`; `SEED_DAY1_SPEAKER_ORDER_LETS_AI_INITIATE_VOTE` × 3 sites and `SEED_AMBIGUOUS_IA_PAIR_ALIVE` × 1 site in `test_slice7_vote.py`) are refactored to monkeypatch the underlying RNG-using helper (`graphia.nodes.day._shuffle_order` for the speech-order pins; the per-Mafioso pointing helper or an existing `target_human_pointing`-style fixture for the Night-1 victim pin). Production code is unchanged.

**Migration rule for every sub-task below (per architecture §6 "Determinism Posture & Testing Conventions" bullet 3 and tech spec §2.7):**

1. Replace `monkeypatch.setenv("GRAPHIA_SEED", str(SEED_<DESCRIPTIVE_NAME>))` with `monkeypatch.setattr(graphia.nodes.day, "_shuffle_order", lambda players, seed: <hand-written-order>)` where `<hand-written-order>` is the deterministic list of alive-player-ids the test needs.
2. The hand-written-order construction differs per site (see tech spec §2.7 for shape per site). Construct the order from the live state at the patch site if needed (e.g., `players` dict passed to the lambda gives access to the role and human-ness of each player).
3. Delete the renamed seed constant once no remaining references.
4. Run the file's tests after each migration to surface real regressions.

- [x] **Sub 4.1:** In `tests/test_slice6_day.py`, refactor the single remaining seed setenv (the site that pins `SEED_HUMAN_MID_DAY_ORDER`) to monkeypatch `graphia.nodes.day._shuffle_order` so the returned order places the human at index ≥3 (so ≥3 AI speakers precede the human's day_turn). Delete the `SEED_HUMAN_MID_DAY_ORDER` constant. **[Agent: testing]**
- [x] **Sub 4.2:** In `tests/test_slice7_vote.py`, refactor the 3 sites pinning `SEED_DAY1_SPEAKER_ORDER_LETS_AI_INITIATE_VOTE` to monkeypatch `graphia.nodes.day._shuffle_order` so the first AI speaker is a Law-abiding (non-target) AI, allowing scripted vote-DayActions to land. Delete the constant. **[Agent: testing]**
- [x] **Sub 4.3:** In `tests/test_slice7_vote.py`, refactor the 1 site pinning `SEED_AMBIGUOUS_IA_PAIR_ALIVE`. This site needs Bianca and Elias both alive after Night 1 so `/vote ia` is ambiguous. The Night-1 victim is chosen by mafia-pointing seeded RNG in `src/graphia/nodes/night.py`, not by `_shuffle_order`. Either reuse the existing `target_human_pointing`-style pattern from `tests/conftest.py` to force a non-Bianca, non-Elias victim, or monkeypatch the night-pointing helper directly. Delete the constant. **[Agent: testing]**
- [x] **Sub 4.4:** `uv run pytest -q` — full suite green. Same 132 passed / 1 skipped baseline. **[Agent: testing]** _(Done after two contingent fixes for previously-unaudited day-order / night-pointing dependencies that surfaced once `GRAPHIA_SEED` became time-based: `tests/test_vote_validation.py` got `_shuffle_order` monkeypatches at 8 sites pinning the human first in the day order; `tests/test_slice9_polish.py::test_ctrl_c_shows_aborted_banner` got the existing `dynamic_night_pointing` fixture wired in to prevent the human being killed Night 1 via the random pointing fallback when the unified Sonnet fake's empty `pointings` queue raised. Final: 132 passed, 1 skipped, stable across 30+ consecutive runs.)_
- [x] **Sub 4.5:** Stale-comment cleanup discovered during the Sub 4.4 contingent fixes. Renamed unused-but-required-by-signature `seed` parameter to `*_` on four `_shuffle_order` stub helpers (`tests/test_slice6_day.py::_human_at_index_three`, `tests/test_slice7_vote.py::_human_then_law_abiding` and `::_human_then_mafia`, `tests/test_vote_validation.py::_shuffle` inside `_human_first_factory`). Updated stale "seed 0" / "human is Law-abiding at seed 0" references in `tests/test_slice7_vote.py` (lines ~356, ~368, ~621) to name `GRAPHIA_ROLE` instead of the seed value. Updated stale "seed 0" mention in the module docstring of `tests/test_slice9_polish.py` (line ~6). Dropped redundant explicit `monkeypatch.delenv("GRAPHIA_SEED", raising=False)` at `tests/test_slice7_vote.py:839` since `tests/conftest.py`'s autouse fixture already handled the unset default. **[Agent: testing]**
- [x] **USER:** Skim the diff for the migrated test files. Two contingent fixes during Sub 4.4 widened the migration: (1) `tests/test_vote_validation.py` got `_shuffle_order` monkeypatches at 8 sites (Sub 3.7's audit missed an implicit day-order dependency); (2) `tests/test_slice9_polish.py::test_ctrl_c_shows_aborted_banner` got the existing `dynamic_night_pointing` fixture (the empty-pointing-queue fallback was hitting time-based RNG and randomly killing the human). A subsequent cleanup pass also caught 4 files that the original Slice 3 grep missed because they used inline literal `"0"` instead of named `SEED_*` constants (`test_app_boot.py`, `test_slice2_roster.py`, `test_slice3_names.py`, `test_day_turn_ui_repro.py` — 7 sites total, all role-only, all migrated to `GRAPHIA_ROLE`). And `tests/test_play_as_role.py` had 2 defensive `GRAPHIA_SEED="0"` setenvs that weren't asserting anything seed-dependent and were dropped. _(Effectively verified: Slice 5's production-side retirement absorbed and superseded this state, and the full suite stayed green across the transition.)_

  Confirm: `grep -rn -B1 -A2 setenv tests/ --include='*.py' | grep GRAPHIA_SEED` should show exactly five `setenv` sites (subject-of-test, not tunneled-through-RNG dependencies):
    - `tests/test_slice4_role_reveal.py:95` (exempt subject-of-test, parametrised seed→role mapping);
    - `tests/test_dual_mode_smoke.py` lines 291-292 and 308-309 (sanctioned cross-mode byte-equality);
    - `tests/test_play_as_role.py:66` (asserts byte-identity at seed 1234 — test's subject);
    - `tests/test_play_as_role.py:117` (needs stable seed across 3 parametrize variants for cross-case identity).

  All other monkeypatched lambdas place the human first (`_human_first_factory`) or human-then-target-other-role-AI, which are sensible orders for the tests' needs.

## Slice 5 — Retire `GRAPHIA_SEED` and `config.seed` from production (§2.7 "Retire `GRAPHIA_SEED` from production; pin only the dual-mode test via `random.seed(...)`")

After this slice, no `GRAPHIA_SEED` env var, no `config.seed` field, no per-call seed-salt arithmetic anywhere in the repo. The cross-mode byte-equality test in `tests/test_dual_mode_smoke.py` pins its trajectory via `random.seed(SEED_DUAL_MODE_DETERMINISTIC_TRAJECTORY)` at the start of each mode's run — the only place in the codebase that talks about seeding the RNG. Two test files (`test_slice4_role_reveal.py` and one test in `test_play_as_role.py`) lose their subject and are deleted; one test refactors to a cleaner mechanism.

**Migration sequencing.** Sub 5.1 (production) and Sub 5.2 (dual-mode test pin) must land together — production changes break the dual-mode test until its pin moves from env-var to `random.seed`. Treat as one atomic landing.

- [x] **Sub 5.1:** Refactor production code to drop the seed concept (per tech spec §2.8):
    - `src/graphia/config.py`: remove `seed: int` field from `GraphiaConfig`; remove `GRAPHIA_SEED` parsing block from `load_config()` (the `seed_raw` lookup, int-coercion, `time.time_ns()` fallback, and the stderr error branch).
    - `src/graphia/nodes/setup.py::assign_roles`: replace `rng = random.Random(config.seed); rng.shuffle(deck)` with `random.shuffle(deck)` in both branches (unset and pinned paths).
    - `src/graphia/nodes/day.py::_shuffle_order`: drop the `seed` parameter; body uses module-global `random.shuffle(ids)`. Drop the docstring's "deterministic per (cycle, round)" claim.
    - `src/graphia/nodes/day.py` call sites (around lines 162, 291, 308, 398, 633): remove `order_seed`/`next_seed` salt arithmetic; call `_shuffle_order(players)`.
    - `src/graphia/nodes/night.py` (around lines 139, 180, 212): replace `random.Random(seed + offset)` constructions with direct `random.choice(...)` / `random.shuffle(...)` against module-global RNG.
    **[Agent: langgraph-agentic]**
- [x] **Sub 5.2:** Refactor `tests/test_dual_mode_smoke.py` to pin determinism via `random.seed(SEED_DUAL_MODE_DETERMINISTIC_TRAJECTORY)` at the start of each mode's run, replacing the two `monkeypatch.setenv("GRAPHIA_SEED", ...)` calls (lines ~291 and ~308). Confirm byte-equality still holds across local and remote runs. The `SEED_DUAL_MODE_DETERMINISTIC_TRAJECTORY = 0` constant stays. **[Agent: testing]**
- [x] **Sub 5.3:** Delete `tests/test_slice4_role_reveal.py` (its subject — seed-→role mapping — no longer exists). Confirm no other test file imports from it. **[Agent: testing]**
- [x] **Sub 5.4:** In `tests/test_play_as_role.py`:
    - Delete `test_default_unset_unchanged_under_seed` and the `EXPECTED_ROLES_SEED_1234` constant. The unset path is now intentionally non-deterministic; no frozen list to regression-guard against.
    - Refactor `test_role_value_is_case_insensitive` to test case-insensitivity at the parsing layer (call `load_config()` directly for each variant and compare the resulting `config.human_role` token), rather than driving through `assign_roles` and comparing role lists across runs. Removes the cross-run-identity dependency on RNG state. **[Agent: testing]**
- [x] **Sub 5.5:** Drop the `seed=0` kwarg from the 3 direct `GraphiaConfig(...)` constructions in `tests/test_remote_mode_smoke.py` (lines ~739, ~1017, ~1130). **[Agent: testing]**
- [x] **Sub 5.6:** Simplify stub helper signatures from `(players, *_)` to `(players)` across:
    - `tests/test_slice6_day.py::_human_at_index_three`
    - `tests/test_slice7_vote.py::_human_then_law_abiding`
    - `tests/test_slice7_vote.py::_human_then_mafia`
    - `tests/test_vote_validation.py::_shuffle`
    Production's `_shuffle_order` now takes one arg, so stubs match. **[Agent: testing]**
- [x] **Sub 5.7:** In `tests/conftest.py`, delete the autouse `monkeypatch.delenv("GRAPHIA_SEED", raising=False)` line (~268) and its accompanying docstring fragment ("Ensures GRAPHIA_SEED is unset unless a test opts in" — no env var to clear). **[Agent: testing]**
- [x] **Sub 5.8:** `uv run pytest -q` — full suite green. Run 5+ consecutive times to confirm cross-run stability under fresh module-global RNG state. **[Agent: testing]** _(Done as part of Sub 5.3–5.7 bundle: 3 consecutive runs green at 129 passed, 1 skipped.)_
- [x] **Sub 5.9:** Documentation updates:
    - `context/spec/005-play-as-role/functional-spec.md` §2.3: drop the "(seeded by `GRAPHIA_SEED` as today)" parenthetical from the unset-path acceptance criterion.
    - `context/product/architecture.md` §6 bullet 3: tighten the wording — `tests/test_dual_mode_smoke.py`'s mechanism is no longer "GRAPHIA_SEED" but `random.seed(...)` in-test, which is the same posture as the other monkeypatching examples. Mention the dual-mode test as the canonical example of in-test `random.seed` pinning.
    - `context/adr/006-test-role-pinning-via-graphia-role.md` §3 Decision: amend the "single deliberate exception is `tests/test_slice4_role_reveal.py`" paragraph — that file is deleted in Slice 5. The ADR's core decision (GRAPHIA_ROLE replaces magic-seed-for-role) stands; the exception list shrinks.
    **[Agent: general-purpose]**
- [x] **Sub 5.10:** Verify `grep -rn 'GRAPHIA_SEED' --include='*.py'` returns zero hits across the whole repo (production + tests). **[Agent: testing]** _(Zero `*.py` hits confirmed. Active docs (`README.md`, `CLAUDE.md`, ADR-006 §6 References) also scrubbed of `GRAPHIA_SEED` references; historical spec docs (`context/spec/001-…/technical-considerations.md` and `002-…/tasks.md`) retain their snapshots of past project state.)_
- [x] **Sub 5.11:** Final stale-concept comment scrub across the test suite, prompted by a case-insensitive `seed` audit. Single-line fixes in 8 files (`tests/test_slice6_day.py`, `tests/conftest.py`, `tests/test_remote_mode_smoke.py`, `tests/test_slice7_vote.py`, `tests/test_app_boot.py`, `tests/test_slice3_names.py`, `tests/test_slice2_roster.py`, `tests/test_slice9_polish.py`) — replaced obsolete concept names like "seeded fallback", "time-seeded random pick", "seed-0 trajectory", "seed is not load-bearing" with their current-code equivalents ("random fallback", "random pick", "full game", "no RNG pinning is required"). Bulk-replaced 7 identical `# ... replaces a previously seed-pinned setup.` comment blocks in `tests/test_vote_validation.py` with a tight one-line equivalent ("Pin the Day-1 speaker order so the human's day_turn surfaces on the first super-step after the name resume."). Tightened the module docstring of `tests/test_vote_validation.py` to drop the historical "vs. seed-based determinism" comparisons. Outcome: 27 remaining `seed` mentions in the test suite, every one legitimate (16 describe the sanctioned `random.seed(SEED_DUAL_MODE_DETERMINISTIC_TRAJECTORY)` in `tests/test_dual_mode_smoke.py`, 11 are idiomatic English "seed" as "populate" — pre-seeded queues, seed the store). **[Agent: testing]**
- [x] **USER:** Manual smoke — launch the game (`make play`, `make play ROLE=mafia`, `make play ROLE=law-abiding`) and confirm gameplay is unchanged from a player's perspective. Each run will now be unrepeatable (no `GRAPHIA_SEED`), which is the intended new normal. _(Confirmed by user.)_
