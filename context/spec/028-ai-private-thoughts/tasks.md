# Tasks: Per-AI Day-Round Private Thoughts (Spec 028)

AI-behavior, gameplay-influencing → gated by a **default-on** flag `GRAPHIA_PRIVATE_THOUGHTS` (ADR 011). Structural injection + privacy are unit-tested; the behavioral effect is **effort-not-results** (CR 005). A **new LLM call site** (the reflection) → the test net (`FakeLargeUnified`) must gain a `Reflection` queue.

Functional spec: `./functional-spec.md` · Technical considerations: `./technical-considerations.md`

> **Shared surface with spec 030** (and the merged 024/025 day-prompt work): 028 and 030 are **NOT disjoint** — both add a default-on flag (`config.py` + `graph.py` `_assemble_graph` threading) and both touch `mafia_point` (028 for its Night `{private_thoughts}` injection, 030 for the shuffle). Implement **sequentially** (or careful merge), NOT in parallel worktrees against each other. Spec 029 is independent.

---

- [x] **Slice 1: AI players write a private end-of-round thought (produced, accumulated, private, shown in the eval transcript)**
  - [x] Reflection production + private storage: a new **mild** `REFLECTION_SYSTEM` + user template in `prompts.py`; a new `day_round_reflect` node in `nodes/day.py` that, at each Day round's close, iterates the surviving **non-human AI** players, calls `get_large()` once each, and returns a private `private_thoughts` delta with **no public `messages`** (the `is_human` seat — incl. the spec-026 scripted seat — is excluded); `GameState.private_thoughts: dict[player_id, list[str]]` with a new custom `_merge_private_thoughts` reducer that **accumulates per player** (pure, copy-not-mutate, not replace); wire the node into the round-wrap in `graph.py` (node self-guard per the tech doc) in **both** `build_graph` and `build_runtime_graph`. **[Agent: langgraph-agentic]**
  - [x] Ablation flag: `GraphiaConfig.private_thoughts_enabled: bool = True` via `_env_flag("GRAPHIA_PRIVATE_THOUGHTS", default=True)`; thread it into the reflect node via `_assemble_graph` partials (both builders) so flag-off skips reflection entirely (no node effect, prompts unchanged). **[Agent: langgraph-agentic]**
  - [x] Extend the test LLM net: add a `Reflection` scripted queue + a `reflections=` kwarg to `FakeLargeUnified` / `fake_large` (replay-last-when-drained, mirroring the existing per-schema queues), and confirm `safe_llm` covers the new reflection `get_large` call site — so a flag-on full-Day test never falls through to real Bedrock. **[Agent: testing]**
  - [x] Eval-transcript rendering: `tools/eval_transcript.py` renders each private thought as its own attributed, private/annotated element (e.g. `<thought player="…">…</thought>`), distinct from public speech, never shown to other players / the live UI. **Verify** the `stream_mode="updates"` payload shape for the custom-reducer `private_thoughts` channel (raw node delta vs post-reduce map) and render accordingly. _(Verified at implementation: `stream_mode="updates"` carries the node's RAW return — this round's new thoughts — NOT the post-reducer accumulated map; the renderer renders the delta's per-player lists directly.)_ **[Agent: langgraph-agentic]**
  - [x] Tests `tests/test_slice28_private_thoughts.py`: a thought is produced per surviving AI at round close (dead + human skipped); the reducer accumulates per player without clobber (purity); the thought is **private** (absent from public `messages` and from any other player's prompt context); the transcript renders the `<thought>` element; flag-off → no reflect node, no thoughts, prompts unchanged; the dual-mode byte-equal smoke stays green (private channel is outside its public-log scope). Full `uv run pytest -q` green. **[Agent: testing]**

- [ ] **Slice 2: Each AI's own accumulated thoughts feed its later decisions**
  - [x] Inject a player's **own** thoughts (event order) into a new `{private_thoughts}` slot in `DAY_SPEAK_USER_TEMPLATE`, `AI_VOTE_USER_TEMPLATE`, and `MAFIA_POINT_USER_TEMPLATE`; a builder gated by the flag (enabled-but-empty → `""`); wire into `_ai_day_action`, `_ai_ballot`, and the Mafioso night-point (`mafia_point` / `_ai_pick_target`), threading the flag + that player's thoughts; both builders. **[Agent: langgraph-agentic]**
  - [x] Tests (extend `tests/test_slice28_private_thoughts.py`): a player's **own** thoughts appear in its Day-speech, vote, and (Mafioso) Night-point prompts, in order; a player **never** receives another player's thoughts (no cross-leak); the three template `.format()` sites render without `KeyError`; flag-off parity (the slots collapse to the pre-028 prompt form). Full `uv run pytest -q` green. **[Agent: testing]**
  - [ ] Verification (effort-not-results, CR 005): `uv run pytest -q` green; then run `make blunder-eval` vs the recorded baseline and log whether a private reasoning channel improves play (cross-day coherence / decisiveness / win-rate) **confirmed or refuted**. Clean-or-commit transcripts first. _(deferred — run on merged main, and ideally after spec 030's night-roster fix so the AI seats aren't killed Night 1.)_ **[Agent: testing]**
