# Tasks: Eval Ledger Viewer (Spec 012)

Vertical slices for [spec 012](./functional-spec.md) per its
[technical-considerations](./technical-considerations.md). The deliverable is a
standalone Textual app (`make view-ledger`) that reads `evals/blunder-ledger.yaml`
and shows it as a scrollable, searchable table with a full-record drill-down —
the reader spec 011 deferred. Two layers: a **pure, Textual-free data layer**
(`src/graphia/eval_ledger.py`) and a **thin Textual viewer**
(`src/graphia/ui/ledger_viewer.py`).

Each slice leaves the viewer runnable with more capability (vertical slicing:
MVP table → search → drill-down). Offline verification is `uv run pytest -q` —
pure unit tests over the data layer + Textual `App.run_test()` (Pilot) tests
over the app (the repo's real TUI-test tool, mirroring `tests/test_app_boot.py`);
no LLM, no network, no AWS (the viewer never imports `load_config`). Agents:
`python-backend` (pure data layer, dependency, docs), `textual-tui` (the Textual
app), `testing` (suites). The **headline risk is heterogeneous records** — the
committed ledger already mixes pre-provenance (`run.games`, no `code`, no CI)
with full records, absorbed by the defensive flattener (tech-spec §2.1, §3).

- [x] **Slice 1: Open the viewer and see all runs as a scrollable table** *(Done 2026-06-13: `make view-ledger` opens the table over the real 6-record ledger; +22 tests, suite 407 → 429. Note: vote metrics are stored as **flat dotted keys** in the YAML — `metrics['self_vote.initiation']`, not nested maps as tech-spec §2.1 first assumed; the flattener resolves flat-first with a nested fallback.)*
  - [x] Add the dependency (`uv add pyyaml`; `uv add --dev types-pyyaml`) and create the pure data layer `src/graphia/eval_ledger.py` per tech-spec [§2.1](./technical-considerations.md) — **no Textual import**: `load_ledger(path)` (`yaml.safe_load_all` over the `---` stream; missing/empty file → `[]`; `None`/trailing docs skipped; malformed YAML → `LedgerParseError`); `METRIC_ORDER` (the canonical `(dotted_key, header_label)` family from `evals/README.md`); `build_table_model(records) -> TableModel` with four **index-parallel** lists (columns / rows / search_blobs / records). The flattener uses **defensive dotted-get with defaults** so heterogeneous records never raise (`settings.games ?? run.games`, missing `code`/CI tolerated, nested `metrics.self_vote.initiation` resolved); cells format `rate [ci_low–ci_high] m/n`, **omit the band** when CI is absent, **empty cell** for an absent metric (distinct from a clean `0.00 […] 0/n`); the `⚠` dirty-marker column from `code.dirty`; `search_blobs` lowercase-concatenate date/provider/both models/commit/branch/clean-or-dirty/**full notes**. Emit plain strings (Rich stays a UI concern). **[Agent: python-backend]**
  - [x] Create the Textual viewer `src/graphia/ui/ledger_viewer.py` per tech-spec [§2.2–2.3](./technical-considerations.md): `LedgerViewerApp(App[None])` (ledger `Path` injected via constructor, default = `LEDGER_PATH` imported from `graphia.tools.blunder_eval`; **does not** call `load_config()`), a `LedgerTableScreen` with a `DataTable(id="ledger-table", cursor_type="row")` at `height: 1fr` populated from `build_table_model` (built-in pinned header on vertical scroll; both-axis scroll; **`fixed_columns=0`** so the whole grid scrolls together; numeric metric columns wrapped `Text(..., justify="right")` at `add_row`), and a `Static(id="empty-state")` shown in place of the table when the ledger is empty/missing ("No runs recorded yet."). Add the `python -m graphia.ui.ledger_viewer` entry (with optional `--path`), the `make view-ledger` target (beside `inspect-diary`, comment-then-target style) + `.PHONY`, and a one-line mention in the README evals section + CLAUDE.md. **[Agent: textual-tui]**
  - [x] Pure unit tests `tests/test_ledger_model.py` (no Textual): `load_ledger` over a multi-doc fixture → N records; missing file → `[]`; empty file → `[]`; trailing-`---`/`None` doc skipped; malformed YAML → `LedgerParseError`; a **pre-provenance fixture** (no `code`, `run.games`, no CI) flattens without raising. `build_table_model`: column order = fixed + canonical metric order; **absent metric → empty cell** vs clean `0.0` → non-empty cell; exact cell format incl. band-omission on pre-CI; `search_blobs` contain date/provider/model/commit/branch/**notes**; index-parallelism (`len(rows)==len(records)==len(search_blobs)`). Run `uv run pytest -q`; suite green. **[Agent: testing]**
  - [x] Pilot UI tests `tests/test_ledger_viewer.py` (mirror `tests/test_app_boot.py`; ledger path injected via constructor → `tmp_path`, never the committed ledger): write a tmp ledger with 2–3 records, `async with LedgerViewerApp(path=tmp).run_test() as pilot` → assert `DataTable.row_count` == record count and a known cell (a date or a formatted metric) is present; an empty/missing ledger → `#empty-state` shows "No runs recorded yet.", no crash, table hidden. Confirm scrolling is exercised (a tall/wide fixture renders without error). Run `uv run pytest -q`; suite green. **[Agent: testing]** *(After this slice: `make view-ledger` opens a scrollable, headed table of runs.)*

- [x] **Slice 2: Filter the runs by search**
  - [x] In `LedgerTableScreen`, add `Input(id="search", placeholder="Filter runs…")` (docked top) + `Static(id="match-count")`, and the filter per tech-spec [§2.4](./technical-considerations.md): `on_input_changed` lowercases the query, keeps row indices where `query in search_blobs[i]`, rebuilds via `table.clear(columns=False)` + re-`add_row`, and maintains a `visible_indices` list (so a later row-select still resolves to the right raw record). Update `#match-count` to "Showing X of N"; empty query → all rows return; a non-empty query with **zero** survivors → hide the table and show `#empty-state` with **"No runs match '<query>'."** (distinct copy from the empty-ledger message). **[Agent: textual-tui]**
  - [x] Pilot tests (extend `tests/test_ledger_viewer.py`): typing into `#search` filters the rows (row_count drops; `#match-count` reads "Showing X of N"); clearing restores all; a no-match query hides the table and shows the distinct "No runs match …" copy. Run `uv run pytest -q`; suite green. **[Agent: testing]** *(After this slice: typing narrows the table live.)*

- [x] **Slice 3: Open a run's full record (drill-down) + read-only guarantee**
  - [x] Add `render_detail(record) -> str` to `src/graphia/eval_ledger.py` per tech-spec [§2.5](./technical-considerations.md): a readable **sectioned** full-record render (not a YAML re-dump) in canonical order `run → code → provider → settings → quality → metrics → notes` — every provenance field (commit, branch, clean/dirty, model ids + ollama digests/`server_version` or bedrock `note`, settings, `metrics_version`), **every present metric's full-precision** rate + `[ci_low–ci_high]` + `count/denominator`, run-quality counts, and the **complete free-text note verbatim** (newlines preserved); absent fields shown as `—`/omitted. Pure, unit-testable. **[Agent: python-backend]**
  - [x] In `ui/ledger_viewer.py`, add `DetailScreen(Screen)` (a `VerticalScroll` rendering `render_detail`) and wire `on_data_table_row_selected` → stash `cursor_row`, resolve the raw record via `visible_indices`, `push_screen(DetailScreen(record))`; `escape`/`q` → `pop_screen`; on the table screen's resume, restore the cursor with `move_cursor(row=stashed, scroll=True)` so the maintainer returns to the same place. **[Agent: textual-tui]**
  - [x] Tests: pure — `render_detail` includes every present metric's exact counts and the full multi-line note (extend `tests/test_ledger_model.py`). Pilot (extend `tests/test_ledger_viewer.py`) — move the cursor to a row, `pilot.press("enter")` → `DetailScreen` is on the stack and shows the note text; `pilot.press("escape")` → back on the table with `table.cursor_row` restored. **Read-only guarantee:** after a full session (filter + drill-down + back), assert the tmp ledger file is **byte-identical** to before (functional-spec §2.5). Run `uv run pytest -q`; suite green. **[Agent: testing]** *(After this slice: the full feature — table → search → select → full record → back, ledger untouched.)*
  - [x] **[User-run, live]** Acceptance walk on the real ledger — `make view-ledger` in a real terminal — **passed 2026-06-14** ("it works well"): the table reads clearly, the field selector + value box scope/filter, the cell cursor scrolls the highlight fully into view, and a run's full record opens and returns (Esc/Backspace). *(Walked with the post-Slice-4–7 UX, which superseded the original typed-filter walk: field selector instead of typed `field:value`, cell cursor, detail Header/Footer chrome.)*

- [x] **Slice 4: Notes column + table-first focus** *(Done 2026-06-14, from live feedback on the real ledger: searching `ollama` matched every row because notes mention "vs ollama" but notes weren't shown — looked like phantom hits; and the docked search Input grabbed initial focus, making the table unnavigable. Folded into spec 012 inline. Suite 437 → 440.)*
  - [x] Data layer: add a single-line **Notes** column — append `"Notes"` to `_FIXED_COLUMNS` (last fixed column, before the metric block, so the UI's right-justify split is undisturbed) + a `_note_cell(record)` that collapses whitespace/newlines and truncates to `_NOTES_CELL_MAXLEN` (tech-spec [§2.3](./technical-considerations.md)). **[Agent: python-backend]**
  - [x] Viewer focus model (tech-spec [§2.2](./technical-considerations.md)): `on_mount` → `table.focus()`; a `/` binding (`action_focus_search`) focuses `#search`; an `on_key` returns focus to the table on `escape` **only while search is focused** (stop the event so the app `escape`→quit fires only from the table); `on_input_submitted` (Enter) jumps to the results; add a `Footer` (`/ Search`, `esc Quit`). **[Agent: textual-tui]**
  - [x] Tests: pure — Notes cell verbatim-short / truncated-long / empty-absent (`tests/test_ledger_model.py`); pilot — table focused on mount + arrow nav, `/`→search→`escape`→table-without-quitting, `escape`-on-table quits (`tests/test_ledger_viewer.py`). Suite green. **[Agent: testing]**

- [x] **Slice 5: Search a specific field (`field:value`)** *(Done 2026-06-14: `provider:bedrock` scopes where free-text `bedrock`/`ollama` over-matches via notes; colon-bearing values like `qwen3-coder:30b` fall back to free-text; multi-term AND. +18 tests, suite 440 → 458. **Superseded same day by Slice 6** — the typed `field:value` syntax matched nothing while the field name was being typed, so a field **selector** replaced it; `row_matches` → `row_matches_field`.)*
  - [x] Data layer (`src/graphia/eval_ledger.py`) per tech-spec [§2.1](./technical-considerations.md) + [§2.4](./technical-considerations.md): add `search_fields` (an index-parallel list of per-row `{field: lowercased_text}` mappings) to `TableModel`; define `SEARCH_FIELDS` (canonical names + aliases: `provider`, `date`, `model` [either model id], `commit`, `branch`, `games`, `note`/`notes`, `state` [clean/dirty]) as the single source of truth; add the pure `row_matches(query, blob, fields) -> bool` — split the query on whitespace, route each `field:value` term (first colon splits; `field` ∈ `SEARCH_FIELDS`) to `fields[field]` and every plain or **unrecognised-prefix** term to `blob`, **AND** all terms, empty query → keep all. The unrecognised-prefix fallback is what keeps a colon-bearing value like `qwen3-coder:30b` searchable. Pure, unit-testable. **[Agent: python-backend]**
  - [x] UI (`src/graphia/ui/ledger_viewer.py`): change `on_input_changed` to keep indices where `row_matches(query, search_blobs[i], search_fields[i])` instead of the inline `query in blob`; everything else (rebuild via `clear(columns=False)` + re-`add_row`, `visible_indices`, `#match-count`, no-match `#empty-state`) is unchanged. **[Agent: textual-tui]**
  - [x] Tests: pure (`tests/test_ledger_model.py`) — `row_matches` plain-term hits blob; `provider:bedrock` scopes; `note:ollama` vs `provider:ollama` disambiguation; multi-term AND; unrecognised `x:y` → free-text so `qwen3-coder:30b` still matches; empty query keeps all; `search_fields` carry each field's text + index-parallel. Pilot (`tests/test_ledger_viewer.py`) — `provider:bedrock` narrows where a bare `bedrock` substring (also in a note) would over-match; `#match-count` tracks; clear restores. Run `uv run pytest -q`; suite green. **[Agent: testing]** *(After this slice: the maintainer can filter to a single field — `provider:bedrock`, `date:2026-06` — not just free-text.)*

- [x] **Slice 6: Field selector instead of typed `field:value` + "back" from detail** *(Done 2026-06-14: a field-selector dropdown [default "All"] replaces typed `field:value`; left/right boundary-jump between selector and value box; Backspace returns from the drill-down. Suite 458 → 463.)*
  - [x] Data layer (`src/graphia/eval_ledger.py`) per tech-spec [§2.1](./technical-considerations.md) + [§2.4](./technical-considerations.md): replace the colon-parsing `row_matches` with selector-driven `row_matches_field(field, value, blob, fields)` (`field == SEARCH_SCOPE_ALL` → whole `blob`, else `fields[field]`; value lowercased + whitespace-split + ANDed; empty value → keep all; no `field:value` parsing so a colon in the value is literal); add `SEARCH_SCOPE_ALL`; trim `SEARCH_FIELDS` to the deduped dropdown order (`provider, date, model, commit, branch, games, note, state` — drop the `notes` alias) and the matching `search_fields` keys. **[Agent: python-backend]**
  - [x] UI (`src/graphia/ui/ledger_viewer.py`) per tech-spec [§2.2](./technical-considerations.md) + [§2.4](./technical-considerations.md): add a `Select(id="field-select")` (default "All", options `[("All", SEARCH_SCOPE_ALL)] + [(f, f) for f in SEARCH_FIELDS]`) to the LEFT of the value `Input` in a horizontal row; a shared `_run_filter()` driven by both `Input.Changed` and `Select.Changed` filtering via `row_matches_field`; **boundary-jump** left/right focus between selector and input (a `SearchInput(Input)` subclass posts a focus-the-selector message when `cursor_position == 0` on `left`; `right` on the collapsed `Select` focuses the input); `escape` returns to the table from either search widget (collapsed); and `Binding("backspace", "close", …)` on `DetailScreen` so **Backspace** ("back") also returns from the drill-down. **[Agent: textual-tui]**
  - [x] DetailScreen chrome (tech-spec [§2.5](./technical-considerations.md)): frame the record with a `Header` (viewer name via `TITLE` + a `SUB_TITLE` = "run detail · Esc / Backspace to go back") and a `Footer` (Esc/Backspace → "Back" shown bindings), so the full-window drill-down stays recognisably the viewer with a visible way back rather than reading like a separate program. **[Agent: textual-tui]**
  - [x] Tests — **update** the now-obsolete Slice 5 typed-scope tests and **add** the selector/nav/back ones. Pure (`tests/test_ledger_model.py`): drop the `row_matches` colon-rule tests; add `row_matches_field` — All→blob free-text, `provider`-scope vs free-text disambiguation (a `bedrock` note doesn't satisfy `provider`-scoped `ollama`), multi-term AND, empty value keeps all, a colon-bearing value (`qwen3-coder:30b`) matched literally; `SEARCH_FIELDS` no longer contains `notes`. Pilot (`tests/test_ledger_viewer.py`): replace the B7 `provider:bedrock` typed tests — set `#field-select` to `provider` + value `bedrock` narrows where free-text `bedrock` (also in a note) keeps two; `Select.Changed` refilters live; `/`-then-`left`-at-caret-0 focuses the selector and `right` returns to the input; `backspace` pops `DetailScreen` back to the table. Run `uv run pytest -q`; suite green. **[Agent: testing]** *(After this slice: pick the field from a dropdown — no typing the field name — and Backspace leaves a run's detail.)*

- [x] **Slice 7: Cell cursor that scrolls fully into view** *(Done 2026-06-14: a highlighted **cell** cursor — `cursor_type="cell"` — auto-scrolls the cell entirely into view as it moves, replacing character-wise horizontal panning; drill-down fires on `CellSelected` and restores the exact cell coordinate. +1 test, suite 464 → 465.)*
  - [x] UI (`src/graphia/ui/ledger_viewer.py`) per tech-spec [§2.2](./technical-considerations.md): switch the `DataTable` to `cursor_type="cell"` (the `DataTable` auto-scrolls the highlighted cell fully into view on every move — both axes); change the drill-down to `on_data_table_cell_selected` (resolve the row via `event.coordinate.row` → `visible_indices`); stash/restore the full **cell coordinate** (`move_cursor(row=…, column=…, scroll=True)`) on resume. **[Agent: textual-tui]**
  - [x] Test (`tests/test_ledger_viewer.py`): on a wide table in a narrow viewport, `cursor_type == "cell"`, panning right to the last column scrolls so the highlighted cell is **fully visible** (and `scroll_offset.x > 0`), panning back to column 0 scrolls home; existing drill-down/back/cursor-restore tests stay green over `CellSelected`. **[Agent: testing]** *(After this slice: navigate cell-by-cell and the highlight is always brought entirely into view.)*

---

_Determinism / suite posture unchanged (architecture §6): the viewer never imports `load_config` or reaches a model, so the mocked suite and `safe_llm` are untouched; every viewer test is offline against a temp ledger. Scrolling is built into `DataTable` (no separate slice). The new `pyyaml` dependency is the parser spec 011 deferred to exactly this increment._

---

## Notes for the implementer

- **`fixed_columns=0` is deliberate** (the maintainer chose "everything scrolls together"). The one-line future toggle to pin the date column is `fixed_columns=1` — leave a comment, don't implement it.
- **No comparison/diff/sort/charts** (functional-spec Out-of-Scope) — the viewer shows the numbers (with CIs) legibly; comparison is by eye. A `--path` flag is the only configurability.
- **Optional ADR** (not a task): adding `pyyaml` + reversing 011's "no YAML lib" stance is foreseen, but `/buddah:adr` can record the `safe_load_all`/read-only choice if wanted.
