# Tasks: Randomized Night-Pointing Roster Order (Spec 030)

Gameplay-fairness + eval-validity fix. Gated by a **default-on** flag `GRAPHIA_NIGHT_ROSTER_SHUFFLE` (ADR 011). The shuffle behavior is **deterministic and precisely unit-tested**; the debias effect (the human's Night-1 over-kill disappearing) is the measured bonus.

Functional spec: `./functional-spec.md` · Technical considerations: `./technical-considerations.md`

> **Shared surface with spec 028**: 028 and 030 are **NOT disjoint** — both add a default-on flag (`config.py` + `graph.py` `_assemble_graph` threading) and both touch `mafia_point` / the Night candidate-list assembly. Implement **sequentially** (or careful merge), NOT in parallel worktrees against each other. Spec 029 is independent.

---

- [x] **Slice 1: The Mafia's Night candidate roster is presented in randomized order (ablatable)**
  - [x] Shuffle seam + replay-safe order + flag: new `_shuffle_night_roster(candidates, *, enabled)` in `src/graphia/nodes/night.py` over the **module-global `random`** (mirroring `_shuffle_order` / `_shuffle_mafia_order` — seedable, monkeypatchable); **when disabled it returns `list(candidates)` BEFORE any `random.*` call** (no draw → preserves the seeded trajectory byte-for-byte — the load-bearing invariant). Compute the shuffled order once in the interrupt-free `mafia_round_start` and freeze it as a `night_law_order` (state) id-list, applied at the single candidate-assembly point so both the AI path (`_roster_lines`) and the human `point`-interrupt `options` see one consistent, replay-safe order. Config `GraphiaConfig.night_roster_shuffle_enabled` via `_env_flag("GRAPHIA_NIGHT_ROSTER_SHUFFLE", default=True)`, threaded via `_assemble_graph` partials into the night node in **both** `build_graph` and `build_runtime_graph`. **[Agent: langgraph-agentic]**
  - [x] Tests `tests/test_slice30_night_roster.py`: the candidate order is shuffled (monkeypatch the seam → differs from insertion order); the candidate **set is preserved** (same players, none added/dropped); **flag-off = insertion order AND no RNG draw** (`random.getstate()` unchanged across the disabled call — the load-bearing test); seed-reproducible when ON (fixed seed → identical order); `load_config()` default-on; `build_runtime_graph` threads the flag (anti-drift); a **replay-safety test on the human-Mafioso `point` path** (which the seed-0 dual-mode smoke does not exercise); re-verify the dual-mode byte-equal smoke stays green (confirm seed 0 still yields a decisive game with the shuffle ON — if not, pick a new documented seed, do **not** special-case the flag). Full `uv run pytest -q` green. **[Agent: testing]**
  - [x] Verification (measured debias): `uv run pytest -q` green; then re-run a measured batch (`make blunder-eval`, random-dealt seat) and confirm the first-created (human) seat's **Night-1 death rate when Law-abiding drops toward chance** (no longer ~50%), removing the positional bias; note this enables a clean **026/027 re-run**. _(deferred — run on merged main.)_ **[Agent: testing]**
