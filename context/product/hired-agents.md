# Specialist Agents Coverage

Generated by `/awos:hire` on 2026-08-29. Re-run `/awos:hire` to refresh — this file goes stale as soon as `.claude/agents/` or `context/product/architecture.md` changes.

## Coverage by Technology

| Technology | Recommended Subagent Role | Status | Agent |
| ---------- | ------------------------- | ------ | ----- |
| Python 3.10+ | `python-backend` | ✅ Covered | python-backend |
| uv / `pyproject.toml` / `uv.lock` | `python-backend` | ✅ Covered | python-backend |
| asyncio (`asyncio.to_thread` driver) | `python-backend` | ✅ Covered | python-backend |
| python-dotenv / `.env` config | `python-backend` | ✅ Covered | python-backend |
| stdlib `random` (module-global, no seed protocol) | `python-backend` | ✅ Covered — corrected this run | python-backend |
| SQLite / `SqliteSaver` checkpointer | `langgraph-agentic` | ✅ Covered | langgraph-agentic |
| LangGraph 1.x (`StateGraph`, reducers, `interrupt()`) | `langgraph-agentic` | ✅ Covered | langgraph-agentic |
| `langchain-aws` / `ChatBedrockConverse` | `langgraph-agentic` | ✅ Covered — corrected this run | langgraph-agentic |
| AWS Bedrock — Nova Pro / Nova Lite gameplay tiers | `langgraph-agentic` | ✅ Covered — corrected this run | langgraph-agentic |
| `langchain-anthropic` / Ollama provider (ADR 010) | `langgraph-agentic` | ✅ Covered — added this run | langgraph-agentic |
| `BedrockEmbeddings` — persona-similarity instrument | `ai-quality-eval` | ✅ Covered — new agent | ai-quality-eval |
| AgentCore Runtime (application side) | `langgraph-agentic` | ✅ Covered | langgraph-agentic |
| AgentCore Gateway / MCP diary surface | `langgraph-agentic` | ✅ Covered | langgraph-agentic |
| AgentCore Memory (per-game + long-term) | `langgraph-agentic` | ✅ Covered | langgraph-agentic |
| AgentCore Observability / CloudWatch traces | `langgraph-agentic` | ✅ Covered | langgraph-agentic |
| Textual (TUI) | `textual-tui` | ⚠️ Partial — no Textual skill exists in the registry; agent uses the `context7` MCP for current docs | textual-tui |
| Rich (styling inside widgets) | `textual-tui` | ⚠️ Partial — same gap as Textual | textual-tui |
| Terraform / OpenTofu | `terraform-aws` | ⚠️ Partial — `terraform-mcp-server` failed to connect on this host | terraform-aws |
| AWS IAM / SSO profile auth | `terraform-aws` | ✅ Covered | terraform-aws |
| pytest (mocked offline suite) | `testing` | ✅ Covered — corrected this run | testing |
| Eval harnesses (`blunder-eval`, `persona-bench`, `repetition-experiment`) | `ai-quality-eval` | ✅ Covered — new agent | ai-quality-eval |
| Quality ledger (`evals/blunder-ledger.yaml`) + transcripts | `ai-quality-eval` | ✅ Covered — new agent | ai-quality-eval |
| Wilson CIs / effort-not-results A/B (CR 005) | `ai-quality-eval` | ⚠️ Partial — no statistics/eval skill in the registry; conventions encoded in the agent prompt | ai-quality-eval |
| Ablatable feature flags (ADR 011) | `langgraph-agentic` | ✅ Covered — added this run | langgraph-agentic |

## Registered Specialist Subagents

| Name | Description | Skills |
| ---- | ----------- | ------ |
| `ai-quality-eval` | Graphia's out-of-suite AI-quality measurement layer — the make-gated eval harnesses (`blunder-eval`, `eval-dialogue`, `repetition-experiment`, `persona-bench`), the metric scorers, the append-only provenance-stamped quality ledger, preserved eval transcripts, Wilson confidence intervals, and effort-not-results A/B measurement (CR 005). Not the mocked pytest suite, game logic, or the viewer's UI. | `modern-python-development`, `pytest-best-practices` |
| `langgraph-agentic` | All LangGraph orchestration — StateGraph design, reducers, `interrupt()`/`Command(resume=…)`, `SqliteSaver` checkpointing, sync vs async execution, the pluggable LLM provider abstraction (ADR 009) and its model configuration (Bedrock Nova via `ChatBedrockConverse`, local Ollama via `langchain-anthropic`), and AgentCore-side application code. Not Terraform IaC, general Python plumbing, or UI rendering. | `langgraph-agentcore` |
| `python-backend` | General Python engineering — project layout, uv/pyproject, type hints, asyncio patterns, stdlib usage, SQLite access, `.env` loading, and refactoring non-LangGraph/non-UI code. Not LangGraph orchestration or Textual UI. | `modern-python-development` |
| `terraform-aws` | Research → Design → Implement → Validate workflow for AWS infrastructure with Terraform. Leverages AWS documentation, Terraform Registry, and live AWS API calls to produce well-architected, convention-compliant IaC. | `terraform-conventions` |
| `testing` | All test-authoring and QA — pytest suites, fixtures, parametrization, mocking the LLM and embeddings boundaries, testing LangGraph nodes in isolation, snapshot-testing Textual screens, and pinning mechanical outcomes by monkeypatching RNG-using helpers. Not production game code, and not the eval harnesses or ledger. | `pytest-best-practices` |
| `textual-tui` | Everything console-UI — Textual app scaffolding, screen and widget composition, Rich styling, async event-loop integration, streaming AI output into a chat panel, the vote-opens-lock-chat transition, and the `make view-ledger` viewer as a Textual surface. Not game logic or general Python plumbing. | (none) |

## Gaps

- Terraform Registry lookups: `terraform-mcp-server` fails to start on this host (`ENOENT: Executable not found in $PATH: docker`) while `terraform-aws.md` lists it as a prerequisite → give the server a Podman-compatible launch command in `.mcp.json`, matching the project's container-runtime-agnostic policy. Until then the agent loses pinned provider/module version lookups and falls back to general knowledge.
- Textual / Rich: no Textual skill exists in the `awos-recruitment` registry (searched 2026-08-29, zero relevant hits) → `textual-tui` fetches current docs through the `context7` MCP instead of a bundled skill.
- Eval statistics and methodology: no evaluation/metrics/statistics skill exists in the registry (top hit `testing-expert`, score 37, is a feature-acceptance-test QA agent — wrong domain) → the ledger contract, Wilson-CI posture, absent-not-zero rule, and effort-not-results principle are encoded directly in `ai-quality-eval`'s prompt rather than in a reusable skill. Consider promoting them to a project-local skill under `.claude/skills/` if a second project needs them.
- `langgraph-agentcore` skill divergence: the installed skill teaches 3-tier model routing, Bedrock Guardrails, and LangSmith observability, all of which this project has deliberately rejected (two tiers; Guardrails descoped per CR 001; no LangSmith) → the override is now stated explicitly in `langgraph-agentic.md`, but the skill itself will keep suggesting them.
